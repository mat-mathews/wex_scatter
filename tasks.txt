Scatter - Implementation Tasks
Based on Product Definition v2.0 (updated 2026-03-04)
Reference: docs/PRODUCT_DEFINITION.md

============================================================================
INITIATIVE 1: MULTIPROCESSING OVERHAUL ✅ PHASES 1-4 COMPLETED (Performance Focus)
Target: 4-15x performance improvement across all phases (Phase 5 optimization deferred)
============================================================================

PHASE 1: File Discovery Operations ✅ COMPLETED (HIGH PRIORITY)
Performance Target: 4-15x improvement
- [x] Parallelize find_changed_files() 
- [x] Parallelize find_csproj_files()
- [x] Parallelize find_files_with_extensions()
- [x] Implement intelligent chunking (75 directories/worker)
- [x] Add automatic fallback to sequential processing
- [x] Maintain 100% backwards compatibility

BENCHMARKING PHASE 1 ✅ COMPLETED (HIGH PRIORITY)
- [x] Create benchmarking framework infrastructure
- [x] Implement timing utilities for function execution
- [x] Create test data generators for various codebase sizes
- [x] Benchmark find_files_with_pattern_parallel() (parallel vs sequential)
- [x] Benchmark find_csproj_files() performance improvements
- [x] Benchmark find_cs_files_with_extensions() performance improvements
- [x] Benchmark find_changed_files() git analysis improvements
- [x] Create performance reporting and visualization
- [x] CRITICAL FIX: Identified and resolved 98%+ performance regression in Phase 1
- [x] Implement intelligent file estimation and adaptive worker scaling
- [x] Validate optimization prevents catastrophic overhead (98%+ improvement achieved)
- [x] Document Phase 1 performance baselines for future comparison
- [x] Comprehensive regression testing (37 unit tests + 10 integration tests)

PHASE 2: Consumer Analysis ✅ COMPLETED (HIGH PRIORITY)
Performance Target: 3-8x improvement
- [x] Phase 2.1: Parallel content analysis for consumer and sproc scanning
- [x] Phase 2.2: Parallel XML parsing of .csproj files in find_consumers()
- [x] Phase 2.3: Parallel project mapping (batch .cs-to-.csproj resolution with dir caching)
- [x] Add worker-based project reference checking
- [x] Optimize batch processing of consumer project candidates

BENCHMARKING PHASE 2 ✅ COMPLETED
- [x] Benchmark Phase 2.1 content analysis (results in benchmark_results/phase21_*)
- [x] Benchmark Phase 2.2 XML parsing parallelization (benchmark_phase22_23.py)
- [x] Benchmark Phase 2.3 project mapping parallelization (benchmark_phase22_23.py)
- [x] Benchmark find_consumers() end-to-end (profile_find_consumers.py)
- [x] Validate correctness: sequential vs parallel results match 100%
- [x] Document Phase 2 performance findings
Note: Phase 2.2/2.3 operations (XML parsing, dir walking) are individually very fast
(microseconds/file). ProcessPoolExecutor startup (~400ms) dominates at test scale.
Parallelization benefits appear at enterprise scale (10,000+ projects). Adaptive
fallback thresholds correctly prevent parallel execution for small datasets.

PHASE 3: Reference Analysis — ✅ COMPLETED BY PHASE 2
Note: The original Phase 3 tasks referenced functions that don't exist in the codebase.
The actual implementation folded this work into find_consumers() steps 2-4, which were
parallelized during Phase 2:
  - "check_project_references()" → done by parse_csproj_files_parallel() (Phase 2.2)
  - "find_using_statements()" → done by analyze_cs_files_parallel() with 'namespace' config (Phase 2.1)
  - "batch processing of .csproj files" → done by parse_csproj_files_batch() (Phase 2.2)
  - "parallel namespace dependency resolution" → done by analyze_cs_files_parallel() (Phase 2.1)
The only remaining sequential work in find_consumers() is method filtering (step 5).

REMAINING WORK: ✅ ALL COMPLETED
- [x] Parallelize method filtering in find_consumers() step 5 (now uses analyze_cs_files_parallel)
- [x] Benchmark find_consumers() end-to-end (profile_find_consumers.py)
Note: find_solutions_for_project() is not in a hot path — called only for reporting.

PHASE 4: Content Analysis — ✅ COMPLETED BY PHASE 2
Note: The original Phase 4 tasks referenced functions that don't exist in the codebase.
The actual implementation:
  - "find_class_usage()" → done by analyze_cs_files_parallel() with 'class' config (Phase 2.1)
  - "extract_class_declarations()" → extract_type_names_from_content() exists but is a
    simple regex on individual file content, called once per changed file in git mode.
    Not a parallelization candidate — it's already fast.
  - "chunked processing of source files" → done by analyze_cs_files_batch() (Phase 2.1)
  - "parallel regex-based type declaration extraction" → covered by Phase 2.1 workers

REMAINING WORK: ✅ ALL COMPLETED
- [x] Profile extract_type_names_from_content() — confirmed not a bottleneck (simple regex, called once per file)
- [x] Parallelize method usage analysis in find_consumers() step 5 (now uses analyze_cs_files_parallel)
- [x] Benchmark content analysis end-to-end (profile_find_consumers.py)

PHASE 5: Optimization (LOW PRIORITY — DEFERRED TO v2.0)
Note: Profiling shows no bottlenecks at current scale. These optimizations
only matter at enterprise scale and can be addressed during v2.0 modularization.
- [ ] Implement memory usage optimization for large codebases
- [ ] Add process pool reuse across multiple parallel calls within find_consumers()
- [ ] Create caching strategies for repeated analysis operations
- [x] Add adaptive worker scaling based on file count (estimate_file_count() implemented)
- [x] Performance profiling and bottleneck identification (profile_find_consumers.py — no bottlenecks found)

============================================================================
INITIATIVE 2: HYBRID GIT ANALYSIS (Accuracy Focus)
Target: Dramatic signal-to-noise improvement in branch analysis
============================================================================

CORE IMPLEMENTATION ✅ COMPLETED
- [x] FR-01: Update Git Mode Flow to fetch specific git diff for each changed file
- [x] FR-02: Create get_affected_symbols_from_diff(file_content, diff_text) function
- [x] FR-03: Implement robust LLM fallback mechanism with try/except
- [x] FR-04: Integrate LLM symbol identification into main Git analysis loop

LLM INTEGRATION ✅ COMPLETED
- [x] Design and test specialized LLM prompt for C# static analysis
- [x] Implement JSON parsing for LLM-returned symbol lists
- [x] Add comprehensive error handling for API failures
- [x] Create logging for LLM calls, identified symbols, and fallbacks

ACCEPTANCE CRITERIA VALIDATION ✅ COMPLETED
- [x] AC-01: Test 1-line method change (should return only affected class)
- [x] AC-02: Test using statement/comment changes (should return empty list)
- [x] AC-03: Test LLM API failure (should fallback gracefully)
- [x] AC-04: Test get_diff_for_file returns valid diff text

============================================================================
TESTING & VALIDATION (HIGH PRIORITY)
============================================================================

PHASE 1 TESTING ✅ COMPLETED
- [x] Create comprehensive test suite (37 tests total)
- [x] Validate multiprocessing implementation
- [x] Add backwards compatibility tests (18 comprehensive tests)
- [x] Add target symbol search tests (classes, methods, stored procedures)
- [x] Create comprehensive testing guide with manual test commands
- [x] Enhance backwards compatibility regression testing coverage

PHASE 2.3 TESTING ✅ COMPLETED
- [x] Unit tests for map_cs_to_projects_batch() (10 tests)
- [x] Unit tests for map_cs_to_projects_parallel() (11 tests)
- [x] Integration tests for sproc mapping (4 tests)
- [x] Total: 25 new tests in test_phase2_3_project_mapping.py
- [x] Full suite: 63 tests passing (38 existing + 25 new)

MULTIPROCESSING TESTING (SEQUENTIAL PRIORITY ORDER)
IMMEDIATE PRIORITY:
- [x] Implement benchmarking harness for performance measurement (benchmark_framework.py)
- [x] Validate Phase 1 performance gains (benchmark_results/phase1_*)
- [x] Create automated before/after comparison utilities (benchmark_analysis.py)

AFTER EACH PHASE: ✅ COMPLETED
- [x] Benchmark Phase 2 end-to-end performance (2.1, 2.2, 2.3 all benchmarked)
- [x] Phase 3/4 covered by Phase 2 — benchmarked and confirmed
- [x] Method filtering (step 5) parallelized
- [x] End-to-end profiling confirms no bottlenecks

ONGOING:
- [x] Add scalability testing across different codebase sizes (test_realistic_workload.py,
      temp_test_data/ with 20-project and 100-project synthetic codebases)
- [ ] Implement resource usage monitoring (CPU, memory, I/O)
- [ ] Create performance regression detection

HYBRID GIT ANALYSIS TESTING ✅ COMPLETED
- [x] Create unit tests for LLM symbol identification (7 tests in test_hybrid_git.py)
- [x] Test fallback mechanism under various failure scenarios
- [x] Test markdown-fenced JSON response handling
- [x] Test invalid JSON response handling
- [x] Test get_diff_for_file with real repo and invalid repo

============================================================================
CONFIGURATION OPTIONS ✅ COMPLETED
============================================================================

MULTIPROCESSING CONTROL
- [x] Add --disable-multiprocessing flag (force sequential)
- [x] Add --max-workers configuration (default: CPU cores + 4, max 32)
- [x] Add --chunk-size configuration (default: 75 directories)

HYBRID GIT CONFIGURATION ✅ COMPLETED
- [x] Add --enable-hybrid-git flag for LLM-enhanced analysis
- [x] Reuse existing --google-api-key and --gemini-model flags
Note: --llm-fallback-mode and --git-diff-context-lines deferred to v2.0

============================================================================
v1.x STATUS (see bottom of file for full validated summary)
============================================================================

COMPLETED:
✅ Phase 1: File discovery parallelization + benchmarking + critical perf fix
✅ Phase 2: Consumer analysis parallelization (2.1, 2.2, 2.3) + 25 new tests
✅ Phase 2.1/2.2/2.3 benchmarking (all phases benchmarked)
✅ Phase 3/4: Method filtering parallelized (step 5 in find_consumers)
✅ End-to-end profiling: no bottlenecks found (profile_find_consumers.py)
✅ 70 tests passing, 100% backward compatibility
✅ Configurable multiprocessing control
✅ Adaptive worker scaling (estimate_file_count)
✅ Initiative 2: Hybrid Git Analysis complete (7 tests in test_hybrid_git.py)

REMAINING v1.x WORK:
📋 Phase 5 optimization (pool reuse, memory — deferred to v2.0 modularization)


============================================================================
============================================================================
SCATTER v2.0 ROADMAP
Source: docs/PRODUCT_DEFINITION.md, Section 12
============================================================================
============================================================================

These initiatives represent the v2.0 evolution from single-file CLI tool
to modular analysis platform with AI integration, impact analysis,
dependency graphing, interactive reporting, and CI/CD integration.

v1 multiprocessing work (Initiatives 1-2 above) feeds directly into v2.0
— the parallelism infrastructure carries forward into the modularized
architecture.

============================================================================
INITIATIVE 3: MODULARIZATION & AI BACKEND FOUNDATION (v2.0 Phase 1)
Goal: Split scatter.py into modular package, establish pluggable AI backend.
Existing v1 functionality must continue to work throughout.
Ref: PRODUCT_DEFINITION.md Section 12, Phase 1
============================================================================

MODULE EXTRACTION ✅ PHASES 1-5 COMPLETED
Phase 1 — Package skeleton + core infrastructure:
- [x] Create scatter/ package directory structure (core/, scanners/, analyzers/,
      ai/, reports/, compat/ with __init__.py files)
- [x] Move multiprocessing infrastructure into scatter/core/parallel.py (10 functions)
- [x] Define shared data models in scatter/core/models.py (constants + regex)
Phase 2 — Scanner + Analyzer extraction:
- [x] Extract file discovery logic into scatter/scanners/file_scanner.py
- [x] Extract .csproj parsing + namespace derivation into scatter/scanners/project_scanner.py
- [x] Extract type declaration extraction into scatter/scanners/type_scanner.py
- [x] Extract sproc detection into scatter/scanners/sproc_scanner.py
- [x] Extract consumer analysis pipeline into scatter/analyzers/consumer_analyzer.py
- [x] Create scatter/compat/v1_bridge.py (solution lookup, batch job mapping, result processing)
Phase 3 — Git analysis + reporters + CLI entry point:
- [x] Extract git analysis into scatter/analyzers/git_analyzer.py
- [x] Move console output into scatter/reports/console_reporter.py
- [x] Move JSON output into scatter/reports/json_reporter.py
- [x] Move CSV output into scatter/reports/csv_reporter.py
- [x] Move CLI to scatter/__main__.py (full argument parsing + mode dispatch)
- [x] Reduce scatter.py to 10-line thin entry point using runpy
Phase 5 — Configuration system + AI task router:
- [x] Create scatter/config.py (AIConfig, ScatterConfig dataclasses, load_config with layered precedence)
- [x] Create scatter/ai/router.py (AIRouter with lazy provider creation, caching, task-type routing)
- [x] Update scatter/__main__.py to use load_config + AIRouter instead of inline GeminiProvider
- [x] Update scatter/__init__.py and scatter/ai/__init__.py exports
- [x] 24 tests in test_config.py (15 config + 3 CLI overrides + 6 router)
- [x] README updated with configuration documentation
Remaining — scatter/_legacy.py contains only AI/Gemini functions (Phase 4 target):
- [ ] Extract namespace analysis into scatter/scanners/namespace_scanner.py (deferred — not yet needed)

BACKWARD COMPATIBILITY ✅ COMPLETED (PHASES 1-4)
- [x] Create scatter/compat/v1_bridge.py that maps old CLI args to new modules
- [x] Preserve scatter.py as thin entry point that delegates to scatter/__main__.py
- [x] Both `python scatter.py` and `python -m scatter` work identically
- [x] Verify all existing CLI flags work identically after modularization
- [x] Run full existing test suite (70 tests) against modularized code — 100% pass
- [x] scatter/_legacy.py: thin backward-compat wrappers for AI functions only
- [x] v1_bridge.py simplified: replaced 4 AI params with single ai_provider=None

AI PROVIDER PROTOCOL ✅ COMPLETED (PHASE 4)
- [x] Define AIProvider protocol/interface in scatter/ai/base.py:
      analyze(prompt, context, task_type) -> AnalysisResult
      supports(task_type) -> bool
      estimate_tokens(context) -> int
      max_context_size -> int
      name -> str
- [x] Define AnalysisResult model (response, confidence, token_usage, cost_estimate)
- [x] Define AI task type enum (summarization, symbol_extraction)

AI TASK ROUTER ✅ COMPLETED (PHASE 5)
- [x] Implement scatter/ai/router.py — provider selection based on task type + config
- [x] Implement fallback logic (try preferred provider, fall back to default)
- [x] Implement task-to-provider mapping from config
- [x] Lazy provider creation with caching for reuse
- [x] Graceful failure — returns None if credentials missing

AI PROVIDERS (TIER 4 — DEFERRED, build on demand)
- [x] Migrate existing Gemini code into scatter/ai/providers/gemini_provider.py
      GeminiProvider extends AIProvider protocol, delegates to module-level functions
      for testability (MagicMock compatibility). scatter/_legacy.py provides thin
      backward-compat wrappers. __main__.py uses ai_provider cleanly (no .model leaks).
- [ ] Implement WEX AI Platform provider (scatter/ai/providers/wex_platform_provider.py)
      — default, no auth required, uses WEX internal AI infrastructure
      Build when WEX AI Platform is available. This is the only provider that matters
      for broad adoption (zero-config, no API key). Others are speculative.
- [ ] Implement Anthropic provider (Claude Opus/Sonnet/Haiku) — requires authorization
- [ ] Implement OpenAI provider (GPT-4o, GPT-4.1) — requires authorization
- [ ] Implement Azure OpenAI provider — requires authorization
- [ ] Implement Ollama provider (local models) — requires authorization

AI RESPONSE CACHING (TIER 4 — DEFERRED, premature optimization)
Rationale: AI calls are infrequent and cheap ($0.01-$0.15/run). Build when cost
becomes a real concern or when latency blocks CI/CD integration.
- [ ] Implement scatter/ai/cache.py — file-based cache with configurable TTL
- [ ] Cache key based on: code content hash + task type + provider
- [ ] Invalidate cache entries when source files change (via mtime or git status)

AI CALL BUDGET & RATE LIMITING ✅ COMPLETED (2026-03-26)
Ref: docs/LEADERSHIP_DESIGN_REVIEW.md (AI Rate Limiting gap)
- [x] Implement scatter/ai/budget.py — AIBudget dataclass (call counting, cap enforcement)
- [x] RateLimitedModel proxy wraps model.generate_content() with budget + backoff
- [x] Exponential backoff with jitter on transient API errors (3 retries, 1-30s delays)
- [x] BudgetExhaustedError caught by task modules' existing except Exception handlers
- [x] --max-ai-calls CLI flag, .scatter.yaml config, SCATTER_MAX_AI_CALLS env var
- [x] Budget wired through AIRouter → GeminiProvider/WexProvider at construction time
- [x] Budget summary logged at INFO after mode dispatch completes
- [x] Warning at 50-call threshold (suppressed when user sets a cap below threshold)
- [x] 22 tests in test_ai_budget.py (budget tracking, proxy, retries, CLI, config)
- [x] AI smoke test in tools/check.sh (exercises real Gemini API when GOOGLE_API_KEY set)
- [x] Mypy clean (5 errors fixed: budget.py, gemini_provider.py, markdown_reporter.py)

TOKEN BUDGET MANAGER (TIER 4 — DEFERRED, premature optimization)
Rationale: Current AI calls are small and targeted. Build when context window
limits become a real constraint (e.g., summarizing 50+ consumer files in one call).
- [ ] Context chunking when input exceeds model window
- [ ] Smart context selection: send relevant files/excerpts, not entire projects
- [ ] Pre-call token estimation and cost logging
- [ ] Configurable per-run token cap (default: 100,000)
- [ ] Warning threshold before proceeding (default: 50,000 tokens)

CONFIGURATION SYSTEM ✅ COMPLETED (PHASE 5)
- [x] Implement scatter/config.py — loads from .scatter.yaml, env vars, CLI flags
- [x] Support repo-level config (.scatter.yaml in repo root)
- [x] Support user-level config (~/.scatter/config.yaml)
- [x] CLI flags override config file values (layered precedence: CLI > repo > user > env > defaults)
- [x] AI provider config: default_provider, task_overrides, provider credentials
- [x] Search config: max_depth, exclude_patterns
- [x] Multiprocessing config: disabled, max_workers, chunk_size
- [x] Env var mapping: GOOGLE_API_KEY, SCATTER_DEFAULT_PROVIDER
- [x] Update __main__.py to use config + AIRouter instead of inline GeminiProvider
- [x] README updated with configuration documentation
- [ ] Budget config: max_tokens_per_run, warn_at_tokens, cache_ttl_hours (deferred — depends on ai/budget.py)

INITIATIVE 3 TESTING — PARTIAL ✅
- [~] Unit tests for each scanner module — PARTIAL: type (48 tests), sproc/db (36 tests),
      project (implicit in test_graph.py/test_new_samples.py). File and namespace scanners untested.
- [~] Unit tests for AI provider protocol compliance — PARTIAL: mock providers tested in
      test_config.py (24 tests) and test_summarize_consumers.py (7 tests). No dedicated protocol test file.
- [x] Unit tests for AI task router (6 tests: default provider, no credentials, task overrides, caching, unsupported task, unknown provider)
- [ ] Unit tests for token budget manager (chunking, estimation) — depends on ai/budget.py
- [ ] Unit tests for AI response cache (TTL, invalidation) — depends on ai/cache.py
- [x] Unit tests for config system (15 tests: yaml loading, precedence, env vars, CLI overrides, malformed yaml, partial config, credentials, task overrides, multiprocessing)
- [x] Unit tests for _build_cli_overrides (3 tests: empty flags, explicit defaults captured, all flags)
- [x] Integration test: full v1 workflow through modularized code (all 132 tests pass)
- [x] Integration test: AI summarization through new provider system (7 tests in test_summarize_consumers.py)
- [x] Backward compatibility: all existing tests pass without modification (132 total)

============================================================================
INITIATIVE 4: CSE IMPACT ANALYSIS & AI ENRICHMENT (v2.0 Phase 2) ✅ COMPLETED
Goal: Work request in, impact analysis out, with AI enrichment.
Depends on: Initiative 3 (modularization + AI backend)
Ref: PRODUCT_DEFINITION.md Section 12, Phase 2
============================================================================

IMPACT ANALYSIS MODE ✅ COMPLETED (HIGH PRIORITY)
- [x] Implement impact analysis CLI mode (--sow / --sow-file)
- [x] Accept --sow "free text description" for inline work requests
- [x] Accept --sow-file path/to/file.txt for full SOW/CSE documents
- [x] Accept --search-scope to specify codebase search directory
- [x] Accept --max-depth for transitive tracing depth control
- [x] Wire impact mode into the analysis pipeline (scanners + analyzers)
- [x] Mutually exclusive with existing modes (--branch-name, --target-project, --stored-procedure)

AI WORK REQUEST PARSING ✅ COMPLETED (HIGH PRIORITY)
- [x] Implement scatter/ai/tasks/parse_work_request.py
- [x] AI extracts analysis targets from natural language:
      project names, sproc names, class names, method names
- [x] Map extracted targets to appropriate analysis modes
      (target-project, sproc tracing, or both)
- [x] Resolve project names to .csproj paths on disk
- [x] Handle ambiguous input gracefully — empty array fallback

AI RISK ASSESSMENT ✅ COMPLETED (HIGH PRIORITY)
- [x] Implement scatter/ai/tasks/risk_assess.py
- [x] Produce risk rating: Low / Medium / High / Critical
- [x] Generate natural language justification for the rating
- [x] Identify specific concerns (e.g., "retry loop with no parameter validation")
- [x] Suggest mitigations for each identified risk

AI COUPLING NARRATIVE ✅ COMPLETED (MEDIUM PRIORITY)
- [x] Implement scatter/ai/tasks/coupling_narrative.py
- [x] Explain why dependencies exist (not just that they exist)
- [x] Identify primary coupling vectors (specific classes, sprocs, shared models)
- [x] Recommend decoupling actions where applicable

AI IMPACT NARRATIVE ✅ COMPLETED (MEDIUM PRIORITY)
- [x] Implement scatter/ai/tasks/impact_narrative.py
- [x] Generate natural language impact summary suitable for non-developers
- [x] Include: affected projects, pipelines, risk level, recommended testing
- [x] Output should be directly shareable (e.g., pasteable into an email or ticket)

ENHANCED BLAST RADIUS ✅ COMPLETED (MEDIUM PRIORITY)
- [x] Implement transitive impact tracing — consumers of consumers, up to N hops
- [x] BFS algorithm with cycle detection
- [x] Add confidence scoring per consumer:
      HIGH = directly uses changed types
      MEDIUM = references the project but may not use the changed code
      LOW = transitive consumer, multiple hops away
- [x] Add depth tracking — show hop distance for each affected project

AI COMPLEXITY ESTIMATE ✅ COMPLETED (MEDIUM PRIORITY)
- [x] Implement scatter/ai/tasks/complexity_estimate.py
- [x] Produce complexity rating: Low / Medium / High / Critical
- [x] Base on: consumer count, transitive depth, unique pipelines, risk ratings
- [x] Include effort estimate range (e.g., "3-5 developer-days")
- [x] Output feeds directly into CSE scoping

REPORTER EXTENSIONS ✅ COMPLETED
- [x] Console: print_impact_report() with formatted output
- [x] JSON: write_impact_json_report() with Path→string serialization
- [x] CSV: write_impact_csv_report() with one row per consumer

PHASE 2 TESTING ✅ COMPLETED (53 tests)
- [x] Unit tests for data model construction (7 tests)
- [x] Unit tests for CLI argument parsing and mutual exclusivity (3 tests)
- [x] Unit tests for AITaskType extensions (2 tests)
- [x] Unit tests for work request parsing (11 tests)
- [x] Unit tests for transitive impact tracing with cycle detection (5 tests)
- [x] Unit tests for risk assessment (5 tests)
- [x] Unit tests for coupling narrative (3 tests)
- [x] Unit tests for impact narrative (4 tests)
- [x] Unit tests for complexity estimate (3 tests)
- [x] Unit tests for console, JSON, CSV reporters (6 tests)
- [x] Integration test: end-to-end with all AI mocked (2 tests)
- [x] All 70 existing tests continue to pass (123 total)

============================================================================
INITIATIVE 5: DEPENDENCY GRAPH, METRICS & DOMAIN ANALYSIS ✅ ALL 6 PHASES COMPLETED
Goal: Full dependency graph with persistence, coupling metrics, domain detection.
Depends on: Initiative 3 (modularization), Initiative 4 (impact analysis)
Ref: PRODUCT_DEFINITION.md Section 12, Phase 3
Detailed plan: docs/INITIATIVE_5_PLAN.md (6 phases, ~2,700 lines est.)
============================================================================

PHASE 1: GRAPH MODEL + BULK BUILDER ✅ COMPLETED (HIGH PRIORITY)
- [x] Create scatter/core/graph.py — ProjectNode, DependencyEdge, DependencyGraph
      DependencyGraph is a pure data structure (mutation, query, traversal, serialization).
      All analysis algorithms are standalone free functions (SRP — no god object).
      Per-node edge indexes (_outgoing/_incoming) for O(degree) lookups.
      Evidence lists capped at 10 entries with evidence_total count.
      ProjectNode stores file_count (not full file paths — not serialized to cache).
- [x] Create scatter/analyzers/graph_builder.py — build_dependency_graph()
      Single-pass O(P+F) construction: parse all .csproj once, scan all .cs once.
      Reverse index for .cs-to-.csproj mapping (sorted prefix match, not per-file dir walk).
      Reuses: find_files_with_pattern_parallel, derive_namespace, extract_type_names_from_content.
- [x] Modify scatter/scanners/project_scanner.py — add parse_csproj_all_references()
      Extracts ALL ProjectReferences, namespace, framework, output type, project style.
- [x] Create test_graph.py — 46 tests (nodes, edges, graph ops, builder, csproj parser, dir index)

PHASE 2: METRICS + CYCLE DETECTION ✅ COMPLETED (HIGH PRIORITY)
- [x] Create scatter/analyzers/coupling_analyzer.py — all standalone free functions
      ProjectMetrics: fan_in, fan_out, instability, coupling_score, shared_db_density, etc.
      compute_all_metrics(graph) — O(N+E) using per-node edge indexes.
      Configurable coupling_weights (default: project_ref=1.0, sproc=0.8, namespace=0.5, type=0.3).
      Weights documented with rationale, overridable in .scatter.yaml.
      rank_by_coupling(metrics, top_n) for top-N most coupled projects.
- [x] Cycle detection via Tarjan's SCC algorithm — O(N+E)
      CycleGroup dataclass: projects, size, shortest_cycle, edge_count.
      detect_cycles(graph) returns List[CycleGroup] (SCCs with size > 1).
      For each SCC, extract one shortest cycle via BFS for illustration.
      NOT DFS back-edge (finds only *a* cycle), NOT full enumeration (exponential).
- [x] Create test_coupling.py — 25 tests (metrics, custom weights, SCC, shortest cycles)

PHASE 3: PERSISTENCE + CLI INTEGRATION ✅ COMPLETED (HIGH PRIORITY)
- [x] Create scatter/store/graph_cache.py — save/load/validate
      Smart git-based invalidation: git diff --name-only checks actual .csproj/.cs changes.
      Non-code-only commits (README, docs) do NOT invalidate the cache.
      Mtime-based fallback for non-git directories.
      Conservative fallback: if git command fails, rebuild.
- [x] Modify scatter/__main__.py — add --graph mode + --rebuild-graph flag
      --graph goes into existing mutually exclusive mode group.
      Flow: check cache → build if stale → save → compute_all_metrics → detect_cycles → report.
- [x] Modify scatter/config.py — add GraphConfig (cache_dir, rebuild, invalidation, coupling_weights)
- [x] Create test_graph_cache.py — 28 tests (roundtrip, smart git invalidation, mtime fallback)

PHASE 4: DATABASE DEPENDENCY MAPPING ✅ COMPLETED (MEDIUM PRIORITY)
Note: Independent of Phases 2 and 3 — can be developed in parallel after Phase 1.
- [x] Create scatter/scanners/db_scanner.py — DbDependency dataclass + detection functions
      Comment stripping preprocessor: _strip_cs_comments() removes // and /* */ before regex.
      Sproc detection with configurable prefixes (default: sp_, usp_; customizable via .scatter.yaml).
      Direct SQL requires string-literal context (opening quote) to prevent false positives.
      EF: DbSet<T> and DbContext subclass detection (reliable patterns).
      Connection string detection in string literals.
      build_db_dependency_matrix() for cross-project shared DB objects.
      add_db_edges_to_graph() for sproc_shared edges.
- [x] Modify scatter/analyzers/graph_builder.py — optional DB dependency step
- [x] Modify scatter/config.py — add DbConfig with sproc_prefixes
- [x] Create test_db_scanner.py — 36 tests (comment stripping, detection patterns, _FileIndex non-class types, integration)

PHASE 5: DOMAIN BOUNDARY DETECTION ✅ COMPLETED (MEDIUM PRIORITY)
- [x] Create scatter/analyzers/domain_analyzer.py — all standalone free functions
      Two-level clustering: connected components first (deterministic, O(N+E)),
      label propagation only for large components (>20 nodes) as refinement.
      Label propagation determinism: sorted iteration order + lowest-label tie-breaking + max 100 iterations.
      Cluster dataclass: name, projects, internal/external edges, cohesion, extraction_feasibility.
      score_extraction_feasibility() based on cross-boundary edges, shared DB, cycles, API surface.
      Review fixes: hoisted sproc map to O(V) construction, cluster_index assigned after sort,
      API surface evidence parsing tested, console column widened for very_hard label.
- [x] Modify scatter/ai/base.py — add BOUNDARY_ASSESSMENT to AITaskType
- [x] Modify scatter/__main__.py — domain clusters auto-run in --graph mode (console + JSON output)
- [x] Update scatter/__init__.py — export Cluster, find_clusters, score_extraction_feasibility
- [x] Create test_domain.py — 15 tests (clustering, label propagation, determinism, naming, feasibility scoring, API surface)

PHASE 6: GRAPH REPORTERS + HEALTH DASHBOARD ✅ COMPLETED (MEDIUM PRIORITY)
- [x] Create scatter/reports/graph_reporter.py — console + JSON output (done in Phase 4/5 merge)
      print_graph_report(), build_graph_json(), write_graph_json_report() — all free functions.
- [x] Add Mermaid output to graph_reporter.py — generate_mermaid() free function.
- [x] Create scatter/analyzers/health_analyzer.py — HealthDashboard, Observation dataclasses
      compute_health_dashboard() aggregates metrics, cycles, clusters, DB hotspots.
      5 deterministic observation rules: stable_core, high_coupling, in_cycle,
      low_cohesion_cluster, db_hotspot. No AI required.
- [x] Add cluster member names to graph console output (top 5 members inline per cluster)
- [x] Add rule-based observations to graph console report
- [x] Add --include-graph-topology flag (default: omit raw graph from JSON to reduce file size)
- [x] Add graph CSV export: one row per project with metrics + cluster assignment
      write_graph_csv_report() — 13 columns including Cluster and ExtractionFeasibility.
- [x] Create test_reporters.py — 21 tests (Mermaid, health dashboard, console output, CSV, JSON topology)
      Review fixes: low_cohesion_cluster threshold bug (ratio vs absolute), dead constant removed,
      test tautology fixed, added coverage for db_hotspot, high_coupling, dotted names, null namespace.

GRAPH BUILDER PERFORMANCE OPTIMIZATION ✅ COMPLETED
Ref: docs/ADR_GRAPH_PERFORMANCE.md, docs/PERFORMANCE_AT_SCALE.md
- [x] Profile graph builder at scale — synthetic codebases at 100/250/500 projects
- [x] Identify hot path: type_usage_edges stage was 91% of build time (O(F×T×S) triple loop)
- [x] Implement inverted index: tokenize identifiers once per file, set intersection with known types
      Replaces per-type regex loop with single-pass O(F×S) — eliminates T dimension entirely
- [x] Add comment stripping to type_usage stage (reuses _strip_cs_comments from db_scanner.py)
      Removes false positives from // and /* */ comments before tokenization
- [x] Fix type name collision bug: type_to_project Dict[str, str] → type_to_projects Dict[str, Set[str]]
      Same type name in multiple projects now creates edges to all owners (was last-writer-wins)
- [x] Benchmark results: 100 projects 172s→1.7s (101x), 250 projects >10min→10.1s, 500 projects 42.8s
- [x] Create synthetic codebase generator (tools/generate_synthetic_codebase.py)
- [x] Create benchmark harness (tools/benchmark_graph_build.py) with per-stage timing
- [x] Identify remaining bottlenecks at 500-project scale:
      Comment stripping 18s (77% of stage 7) — char-by-char Python loop over 232MB
      Label propagation 21s — get_edges_between() called 205K times/iteration, linear scan
      Type extraction 14s — sequential file reads + 3 regex per file

INCREMENTAL GRAPH UPDATES ✅ COMPLETED (Tier 1.5 — prerequisite for CI/CD governance)
Ref: docs/INCREMENTAL_GRAPH_BRAINSTORM.md (team debate), docs/INCREMENTAL_GRAPH_PLAN.md (4-phase plan)
Replace all-or-nothing cache invalidation with surgical per-file patching.
Target: PR with 10 changed files, 500 projects → <1s (vs 60s full rebuild).
Branch: feature/incremental-graph-updates

Phase 1 — Cache format v2 + data models ✅ COMPLETED:
- [x] FileFacts dataclass (types_declared, namespaces_used, sprocs_referenced, content_hash)
- [x] ProjectFacts dataclass (namespace, project_references, csproj_content_hash)
- [x] v2 cache envelope with file_facts, project_facts, project_set_hash
- [x] v1 → v2 migration (load v1 → full rebuild → save as v2)
- [x] Tests: v2 roundtrip, v1 backward compat, project_set_hash mismatch

Phase 2 — Graph mutation + fact extraction ✅ COMPLETED:
- [x] Add remove_edges_from() and remove_edges_to() to DependencyGraph
      O(degree) single-pass with id() sets (not O(n²) list.remove)
      Updates all 4 indexes: _outgoing, _incoming, _forward, _reverse
- [x] extract_file_facts() and extract_project_facts() in graph_patcher.py
- [x] Extend build_dependency_graph() with capture_facts=True option
      Facts captured inline during Step 4 loop (single file read, no second pass)
- [x] Tests: edge removal updates all 4 indexes, fact extraction correctness

Phase 3 — Patch algorithm ✅ COMPLETED:
- [x] Create scatter/store/graph_patcher.py — patch_graph() core function (~640 lines)
- [x] Decision tree: classify → structural check → threshold check → early cutoff → rebuild edges
- [x] Content hash early cutoff (Bazel-inspired: unchanged hash → skip re-extraction)
- [x] Early cutoff on types_declared (Salsa-inspired: declarations unchanged → cheap path)
- [x] Safety valve: >50 projects or >30% files → fall back to full rebuild
- [x] Edge rebuild helpers: namespace, type_usage, sproc, project_reference
- [x] get_changed_files() git diff integration
- [x] Shared regex patterns in scatter/core/patterns.py (prevents divergence)
- [x] _build_project_to_files() index built once (avoids O(all_files) per-project scan)
- [x] Shallow-copy input dicts to prevent partial mutation on bail-out
- [x] project_set_hash validated at patch entry point
- [x] Tests: usage-only, declaration change, .csproj change, deleted/new files, thresholds

Phase 4 — Integration + property tests ✅ COMPLETED:
- [x] Wire into build_graph_context() in graph_enrichment.py
      Handles: v2+git_head → incremental, v2+no_git → mtime fallback, v1 → full rebuild
- [x] Property-based tests: incremental == full rebuild for 6 mutation types
      (usage-only, declaration change, deleted file, new file, namespace change, .csproj ref change)
- [x] Regression: coupling scores and cycles identical after noop patch (integration test)
- [x] Updated all callers of load_and_validate for 5-tuple return type

BENCHMARKING — INCREMENTAL GRAPH UPDATES ✅ COMPLETED:
- [x] Create tools/benchmark_incremental.py — automated perf benchmark
      Generates synthetic codebases at configurable scales (small/medium/large/xlarge)
      9 mutation scenarios: usage-only (1/5/10 files), declaration (1/5 files),
      new file (1/5), deleted file, csproj modified
      Reports patch time, speedup factor, files processed, projects affected
      Supports --runs for median timing, --json/-o for machine-readable output
- [x] Benchmark results (small=100 projects, medium=250 projects):
      Usage-only 1 file:     10ms (110-954x speedup)
      Usage-only 10 files:   70-315ms (16-30x speedup)
      csproj modified:       9-38ms (122-253x speedup)
      Declaration change:    555-7060ms (1.3-2x speedup — triggers global type_usage rebuild)

FILES CHANGED (10 modified, 2 new):
  New: scatter/store/graph_patcher.py, scatter/core/patterns.py
  New: test_graph_patcher.py (49 tests), tools/benchmark_incremental.py
  Modified: scatter/store/graph_cache.py, scatter/core/graph.py,
            scatter/analyzers/graph_builder.py, scatter/analyzers/graph_enrichment.py,
            scatter/__main__.py, test_graph_cache.py

TRANSPARENT GRAPH ACCELERATION ✅ ALL PHASES COMPLETED
Ref: docs/ADR_TRANSPARENT_GRAPH.md

Phase A — Auto graph loading ✅ COMPLETED
- [x] Load graph cache automatically on every run (no flag needed)
- [x] Enrich results transparently when graph is available
- [x] Add "graph_enriched" field to JSON metadata for downstream parsers
- [x] --no-graph escape hatch to skip auto-loading

Phase B — Graph-accelerated consumer lookup ✅ COMPLETED
- [x] Add graph: Optional[DependencyGraph] parameter to find_consumers()
- [x] When graph available, replace stages 1-2 with O(1) reverse lookup
- [x] Class/method text-search still runs on candidate set
- [x] Target-not-in-graph fallback to filesystem
- [x] FilterStage.source field ("filesystem" or "graph") with [graph] annotation
- [x] Parametrized integration tests for correctness verification

Phase C — First-run graph build ✅ COMPLETED
- [x] After find_consumers() completes, build graph cache if none exists
- [x] _ensure_graph_context() idempotent helper at all 4 enrichment sites
- [x] Enrich results on very first run (no second run needed)
- [x] --no-graph escape hatch prevents first-run build
- [x] Silent failure (DEBUG log) if build fails

DEFERRED TO FUTURE INITIATIVE:
- [ ] diff(baseline, current) -> GraphDiff — needs clear use case + full GraphDiff spec
- [ ] Graph query DSL — ad-hoc traversal language, premature before basic graph is proven

INITIATIVE 5 TESTING SUMMARY (220 tests completed across Phases 1-6 + incremental, all merged to main):
- [x] test_graph.py — 46 tests (Phase 1: data models, graph ops, builder, csproj parser, dir index)
- [x] test_coupling.py — 25 tests (Phase 2: metrics, custom weights, Tarjan's SCC, shortest cycles)
- [x] test_graph_cache.py — 28 tests (Phase 3: save/load, smart git invalidation, mtime fallback)
- [x] test_db_scanner.py — 36 tests (Phase 4: comment stripping, detection patterns, _FileIndex non-class types, integration)
- [x] test_domain.py — 15 tests (Phase 5: two-level clustering, label propagation, determinism, feasibility, API surface)
- [x] test_reporters.py — 21 tests (Phase 6: Mermaid, health dashboard, console output, CSV export, JSON topology)
- [x] test_graph_patcher.py — 49 tests (incremental updates: v2 cache, edge removal, fact extraction, patch algorithm, property tests, integration)

============================================================================
INITIATIVE 6: REPORTING & EXTRACTION PLANNING — PHASES 1-4 ✅ COMPLETED (v2.0 Phase 4)
Goal: Fix report quality issues, add missing formats, extraction planning, baselines.
Phases 5-6 (unified report model, diff reports) deferred to Tier 3/4.
Depends on: Initiative 5 (dependency graph + metrics)
Ref: PRODUCT_DEFINITION.md Section 12, Phase 4
Evaluation: docs/OUTPUT_REPORT_EVALUATION.md
============================================================================

PHASE 1: REPORT QUALITY FIXES ✅ COMPLETED (HIGH PRIORITY)
Ref: OUTPUT_REPORT_EVALUATION.md, Priority Recommendations 1-9
Correctness and DX issues in the existing reporters. No new features.

Step 1a — JSON serialization fixes:
- [x] Fix ConsumerFileSummaries: native JSON object, not stringified
- [x] Fix ConsumingSolutions: proper JSON array instead of CSV string
- [x] Use null instead of "" for absent optional fields (PipelineName, BatchJobVerification)

Step 1b — Report metadata:
- [x] Add metadata block to all JSON output (legacy, impact, graph)
- [x] Add scatter version constant (scatter/__version__.py)

Step 1c — Console polish:
- [x] Replace "done." with informative completion summary
- [x] Drop "Type/Level: N/A (Project Reference)" — omit when no class/method filter
- [x] Add per-target consumer count in console

Step 1d — CSV cleanup:
- [x] Omit ConsumerFileSummaries column from legacy CSV
- [x] Use semicolons for list fields in impact CSV (Solutions, CouplingVectors)
- [x] Graph CSV export now handled by Initiative 5 Phase 6

Step 1e — Testing:
- [x] test_report_quality.py — 24 tests (JSON serialization, metadata, console polish, CSV cleanup, version constant, redaction)

PHASE 2: FILTER PIPELINE VISIBILITY ✅ COMPLETED (HIGH PRIORITY)
Ref: OUTPUT_REPORT_EVALUATION.md, Addendum Section A
The multi-stage filter pipeline (discover → project_ref → namespace → class → method) already
runs in consumer_analyzer.py but its intermediate counts are discarded. Surface them.

- [x] Create FilterStage + FilterPipeline dataclasses in scatter/core/models.py:
      FilterStage: name, input_count, output_count, dropped_count property
      FilterPipeline: search_scope, total_projects_scanned, total_files_scanned, stages,
      target_project, target_namespace, class_filter, method_filter,
      format_arrow_chain() method, filter_value_for_stage() method
      Stage name constants: STAGE_DISCOVERY, STAGE_PROJECT_REFERENCE, STAGE_NAMESPACE,
      STAGE_CLASS, STAGE_METHOD + STAGE_LABELS and STAGE_INPUT_LABELS dicts
- [x] Modify consumer_analyzer.py find_consumers() to populate FilterPipeline
      Return type changed from List[Dict] to Tuple[List[Dict], FilterPipeline]
      Capture counts at each stage (post-discovery, post-project_ref, post-namespace,
      post-class, post-method). All return paths return (results, pipeline).
      Updated all callers: __main__.py (3 sites), impact_analyzer.py (2 sites),
      test files (19+ sites), profile script (1 site)
- [x] Surface filter pipeline in all three output formats:
      Console: print_filter_pipeline() — "Search scope: . (scanned 200 projects, 1847 files)"
              "Filter: 200 → 12 project refs → 8 namespace → 4 class match"
      JSON: top-level "filter_pipeline" object with stages array (uses dataclasses.asdict)
      CSV: # comment header with search scope and arrow chain
- [x] When zero results: show pipeline to help debug with diagnostic hint:
      "Hint: 0 of 8 namespace-matching projects contained 'WidgetFactory' — verify the class name"
- [x] Tests: 21 tests in test_filter_pipeline.py (data model, integration, console, JSON, CSV)

PHASE 3: BLAST RADIUS TREE VIEW ✅ COMPLETED (HIGH PRIORITY)
Ref: OUTPUT_REPORT_EVALUATION.md, Addendum Section B
The impact mode console output shows a flat list with depth numbers. Render as a tree instead.

- [x] Add tree rendering to console_reporter.py print_impact_report():
      GalaxyWorks.Data  (target)
      ├── GalaxyWorks.WebPortal       [HIGH]  direct
      │   └── GalaxyWorks.BatchProcessor  [MEDIUM]  via WebPortal
      ├── MyGalaxyConsumerApp         [MEDIUM] direct
      └── MyGalaxyConsumerApp2        [LOW]    direct
      Requires grouping consumers by their parent in the BFS traversal.
- [x] Add propagation_tree to impact JSON output:
      Nested structure preserving the BFS parent chain. Flat consumers[] stays for compatibility.
- [x] Modify impact_analyzer.py to track BFS parent for each consumer
      (which consumer exposed this transitive consumer). Currently only tracks depth.
- [x] Tests: tree rendering with multiple targets, mixed depths, single-target simple case
- [x] Add propagation_parent field to EnrichedConsumer dataclass
- [x] Create scatter/core/tree.py — shared build_adjacency() + CONFIDENCE_LABEL_RANK
      Eliminates DRY violation between console (EnrichedConsumer objects) and JSON (dict) reporters.
      Orphan re-parenting: consumers whose propagation_parent references a removed consumer attach to root.
- [x] Add PropagationParent column to CSV impact report
- [x] 20 tests in TestBlastRadiusTreeView class (model, BFS, orphan, console tree, JSON tree, CSV)

PHASE 4: MARKDOWN OUTPUT FORMAT ✅ COMPLETED (MEDIUM PRIORITY)
Ref: OUTPUT_REPORT_EVALUATION.md, Addendum Section E
Detailed plan: docs/INITIATIVE_6_PHASE4_PLAN.md

- [x] Create scatter/reports/markdown_reporter.py — all free functions
      Build/write separation: build_markdown(), build_impact_markdown(), build_graph_markdown()
      return strings; write_markdown_report(), write_impact_markdown_report(),
      write_graph_markdown_report() are thin file-writing wrappers.
      Shared helpers: _escape_cell(), _fmt_metadata(), _fmt_pipeline(), _write_file(), _md_table().
      Reuses render_tree() from console_reporter.py for blast radius tree in fenced code blocks.
      Reuses generate_mermaid() from graph_reporter.py for dependency diagrams.
- [x] Add --output-format markdown support in __main__.py
      Added 'markdown' to --output-format choices. Stdout fallback when --output-file omitted
      (unlike csv/json which require --output-file). Extracted _require_output_file() helper
      to eliminate 6 repeated validation blocks.
- [x] Legacy mode: table with Consumer, Path, Pipeline, Solutions columns + summary
- [x] Impact mode: risk/complexity header + blast radius tree (```text code block) +
      consumer detail table (7 columns) + complexity/narrative sections
- [x] Graph mode: summary stats table + top coupled table + cycles list + clusters table +
      observations list + Mermaid dependency diagram (inline fenced block)
      GitHub, ADO, and Confluence all render Mermaid natively.
- [x] Tests: 39 tests in test_markdown_reporter.py — escape helpers, legacy/impact/graph builders,
      column-count validation, table edge cases, pipe-in-field escaping, narrative absence,
      CLI dispatch (require_output_file, markdown stdout fallback), file I/O
- [x] Review fixes: renamed _render_tree → render_tree (public cross-module API),
      _md_table row padding for short rows, consistent metadata footer pattern,
      proper type annotations on graph builder params, top-level imports (no lazy loading)

PHASE 5: UNIFIED REPORT DATA MODEL (TIER 4 — DEFERRED)
Ref: OUTPUT_REPORT_EVALUATION.md, Addendum Section H
Rationale for deferral: This is an internal refactor that no user sees. The current
four output formats (console, JSON, CSV, markdown) all work. Do this when a 5th format
is needed or when schema inconsistency blocks a concrete feature.
Currently: legacy modes pass List[Dict], impact mode passes ImpactReport, graph mode builds
an ad-hoc dict in __main__.py. Each reporter picks fields independently. Schema is inconsistent
(PascalCase vs snake_case, stringified JSON vs native objects, flat vs nested).

- [ ] Create AnalysisReport dataclass in scatter/core/models.py:
      mode: str — "git", "target", "sproc", "impact", "graph"
      metadata: ReportMetadata — timestamp, version, cli_args, duration, search_scope
      filter_pipeline: Optional[FilterPipeline]
      targets: List[TargetResult] — normalized target→consumers for all modes
      graph_summary: Optional[GraphSummary] — for graph mode
      impact_summary: Optional[ImpactSummary] — for impact mode
- [ ] Normalize legacy mode results into AnalysisReport (replaces List[Dict] pipeline)
      Adopt snake_case field names. Old PascalCase JSON schema becomes a v1 compatibility option.
- [ ] Update all reporters to accept AnalysisReport
      Each reporter formats the same object differently — adding new formats is trivial.
- [ ] Add --schema-version flag (default: "v2"). v1 produces legacy PascalCase flat JSON.
      Allows downstream consumers to migrate on their own timeline.
- [ ] Tests: AnalysisReport construction from each mode, roundtrip serialization, v1 compat

PHASE 6: DIFF REPORTS (TIER 3 — after CI/CD gates and adoption established)
Ref: OUTPUT_REPORT_EVALUATION.md, Addendum Section C
Depends on: Phase 5 (unified report data model) or at minimum stable JSON schema.
Rationale for Tier 3: Needs real baseline history from actual users. Without
adoption, diff reports are built against synthetic data.

- [ ] Create scatter/analyzers/diff_analyzer.py — compare two AnalysisReport or graph JSON files
      DiffReport dataclass: added/removed projects, coupling deltas, new/resolved cycles,
      cluster feasibility changes, new/lost consumers per target.
- [ ] Add --compare-to <previous_report.json> CLI flag
      Loads previous report, runs current analysis, produces diff.
- [ ] Diff output in all formats:
      Console: "Projects: 108 → 112 (+4). Coupling: GalaxyWorks.Data 9.5 → 12.3 (+29%)"
      JSON: full DiffReport with before/after values
      Markdown: summary table + highlighted changes
- [ ] Tests: known deltas between two synthetic reports, edge cases (empty baseline, removed projects)

BASELINE SAVE/LOAD (HIGH PRIORITY)
- [ ] Implement scatter/store/baseline.py
- [ ] `scatter baseline save --scope . --name "q1-2026-start"` — snapshot graph + metrics
- [ ] `scatter baseline list` — show saved baselines with dates
- [ ] Store as versioned JSON in .scatter/baselines/ directory

BASELINE DIFF (HIGH PRIORITY)
- [ ] `scatter baseline diff --name "q1-2026-start" --scope .`
- [ ] Compare current graph against saved baseline (uses diff_analyzer.py from Phase 6)
- [ ] Compute deltas: projects added/removed, coupling changes, new/resolved cycles
- [ ] Output as console, JSON, markdown, or HTML

AI PROGRESS NARRATIVE (MEDIUM PRIORITY)
- [ ] Implement scatter/ai/tasks/progress_narrative.py
- [ ] Summarize what changed since baseline in natural language
- [ ] Assess whether modernization is on track
- [ ] Flag regressions (new coupling, new cycles)

HTML REPORT ENGINE (TIER 3 — after CI/CD and adoption established)
- [ ] Implement scatter/reports/html_reporter.py
- [ ] Self-contained single HTML file — embedded JS/CSS, no external dependencies
- [ ] Works in any browser, can be emailed or dropped into SharePoint/Confluence
- [ ] Report sections:
      - Executive summary (AI-generated narrative, 1 paragraph)
      - Impact table (sortable, searchable, all affected projects with metrics)
      - Risk assessment (AI-generated, color-coded severity)
      - Database dependency map
      - Pipeline impact
      - Recommended actions (AI-generated)
      - Detailed findings (collapsible per-project sections)

DEPENDENCY GRAPH VISUALIZATION (MEDIUM PRIORITY)
- [ ] Interactive D3.js dependency diagram embedded in HTML report
- [ ] Zoomable, filterable — click a project to see its connections
- [ ] Color-code by domain cluster or coupling score
- [ ] Highlight affected projects in blast radius analyses

METRIC DASHBOARD VIEW (MEDIUM PRIORITY)
- [ ] Color-coded health indicators per project
- [ ] Sortable project table with all metrics columns
- [ ] Filterable by domain cluster, coupling threshold, risk level

EXTRACTION PLANNING MODE (TIER 3 — high strategic value, needs graph integration first)
- [ ] Implement `scatter extract` CLI command
- [ ] Accept --project to specify extraction target
- [ ] Output extraction scope analysis:
      - API surface (types/methods used by external consumers)
      - Consumer inventory (projects that need to switch to service calls)
      - Shared data dependencies (DB objects needing ownership decisions)
      - Circular dependencies to break before extraction
      - Complexity estimate (Low / Medium / High / Critical)

AI MIGRATION STRATEGY (MEDIUM PRIORITY)
- [ ] Implement scatter/ai/tasks/migration_strategy.py
- [ ] Step-by-step extraction approach with sequencing
- [ ] Risk mitigation recommendations per step
- [ ] Identify what to extract first, what to extract last

AI CONTRACT GENERATION (LOW PRIORITY)
- [ ] Implement scatter/ai/tasks/contract_gen.py
- [ ] Draft interface definitions for extraction boundaries
- [ ] Generate based on actual API surface detected by analysis

MERMAID OUTPUT ✅ COMPLETED (2026-03-16)
Core rendering shipped in Initiative 5 Phase 6 (generate_mermaid() in graph_reporter.py).
CLI wiring completed: --output-format mermaid in graph mode, stdout or --output-file.
- [x] generate_mermaid() in scatter/reports/graph_reporter.py (shipped Phase 6)
- [x] --output-format mermaid added to CLI choices
- [x] Graph mode dispatch: stdout or file output
- [x] Non-graph mode rejection with early validation
- [x] Dependency diagrams as Mermaid markup
- [x] Suitable for embedding in Confluence, wikis, READMEs
- [x] Support cluster grouping in Mermaid subgraphs (shipped Phase 6)
- [x] 3 new tests in test_reporters.py (file output, clusters, CLI rejection)

INITIATIVE 6 TESTING
- [x] Unit tests for JSON serialization fixes (Phase 1) — 24 tests in test_report_quality.py
- [x] Unit tests for filter pipeline (Phase 2) — 21 tests in test_filter_pipeline.py
- [x] Unit tests for tree rendering (Phase 3) — 20 tests in TestBlastRadiusTreeView
- [x] Unit tests for markdown output (Phase 4) — 39 tests in test_markdown_reporter.py
- [ ] Unit tests for AnalysisReport model (Phase 5)
- [ ] Unit tests for diff analyzer (Phase 6)
- [ ] Unit tests for baseline save/load (roundtrip, versioning)
- [ ] Unit tests for baseline diff (known deltas)
- [ ] Unit tests for HTML report generation (valid HTML, sections present)
- [x] Unit tests for Mermaid output (valid Mermaid syntax) — 5 tests in test_reporters.py (Phase 6) + 3 new (file output, clusters, CLI)
- [ ] Integration test: `scatter extract` against example projects
- [ ] Integration test: baseline save → make changes → baseline diff
- [ ] Integration test: full HTML report generation from analysis results
- [ ] Visual review: open HTML report in browser, verify interactivity

============================================================================
ADOPTION & CREDIBILITY FIXES (Tier 1 — do before all other remaining work)
Goal: Fix broken features, integrate existing systems, enable adoption.
These are the highest-ROI items on the entire roadmap: low effort, high impact.
============================================================================

FIX --summarize-consumers WIRING ✅ COMPLETED (2026-03-14)
- [x] Wired AI summarization into all 3 legacy modes (git, target, sproc)
- [x] Added _summarize_consumer_files() helper in __main__.py
- [x] Extracted shared SUMMARIZATION_PROMPT_TEMPLATE to scatter/ai/base.py
- [x] Per-file progress logging, stem collision handling (keyed by path)
- [x] 7 tests in test_summarize_consumers.py

INTEGRATE DEPENDENCY GRAPH INTO ANALYSIS MODES ✅ COMPLETED (2026-03-14)
- [x] New --graph-metrics flag enriches consumer results with coupling score,
      fan-in/out, instability, and cycle membership from the dependency graph
- [x] GraphContext dataclass in scatter/analyzers/graph_enrichment.py
- [x] Post-processing enrichment (enrich_legacy_results, enrich_consumers)
- [x] Works in all modes: git, target, sproc, impact
- [x] Schema stability: reporters include graph columns when flag is present
- [x] Graceful degradation: graph failure → warning, analysis continues
- [x] 17 tests in test_graph_enrichment.py

CLAUDE CODE SKILLS ✅ COMPLETED (2026-03-19)
Ref: docs/CLAUDE_INTEGRATION_EVALUATION.md, docs/CLAUDE_SKILLS.md
- [x] Create tools/claude-skills/scatter-graph/SKILL.md
- [x] Create tools/claude-skills/scatter-consumers/SKILL.md
- [x] Create tools/claude-skills/scatter-impact/SKILL.md
- [x] Create tools/claude-skills/scatter-sproc/SKILL.md
- [x] Create tools/claude-skills/scatter-branch/SKILL.md
- [x] Setup script: tools/setup-claude-skills.sh (symlinks into .claude/skills/)
- [x] Smoke test: tools/smoke-test-claude-skills.sh (4/4 pass, impact skipped without API key)
- [x] Documentation: docs/CLAUDE_SKILLS.md
- [x] Team review: dropped stdout (JSON requires --output-file), added $$ to temp filenames,
      shell injection fix (--sow-file instead of inline --sow), error handling on all skills,
      disable-model-invocation on sproc/branch to avoid auto-pick confusion,
      broader trigger phrases in descriptions
Note: Skills live in tools/claude-skills/ (visible, versionable) and are symlinked into
.claude/skills/ via setup script. scatter-graph, scatter-consumers, scatter-impact are
auto-invoked by Claude; scatter-sproc and scatter-branch are manual-only slash commands.

DEPLOYMENT CHECKLIST / SCATTER PIPELINES SHORTCUT ✅ COMPLETED (2026-03-15)
Ref: docs/USE_CASE_REVIEW.md, Use Case #9; docs/PIPELINES_SHORTCUT_PLAN.md
Merged: PR #2 (feature/pipelines-shortcut), commit fd58bfb
- [x] Add --output-format pipelines (sorted unique pipeline names, one per line)
- [x] Support with --branch-name, --target-project, --sow modes
- [x] Clean output (no headers, no decoration) for scripting
- [x] Graph mode rejection with early validation
- [x] 14 tests in test_pipeline_reporter.py

============================================================================
INITIATIVE 7: CI/CD INTEGRATION & WORKFLOW (v2.0 — Tier 2, promoted)
Goal: Integrate Scatter into CI/CD for continuous, automated analysis.
Dependency: Tier 1 items above should ship first for credibility.
Previously depended on Initiative 6 completion — now promoted because CI/CD
gates make scatter infrastructure (runs without anyone remembering to use it).
Ref: PRODUCT_DEFINITION.md Section 12, Phase 5
============================================================================

CI/CD CHECK MODE (HIGH PRIORITY — Tier 2, ship after Tier 1)
Ref: OUTPUT_REPORT_EVALUATION.md, Addendum Section F
- [ ] Machine-readable output with exit codes for pipeline integration
      Exit 0 = no concerns, Exit 1 = threshold violated
- [ ] --fail-on flag with multiple trigger types:
      --fail-on cycles              (any circular dependencies found)
      --fail-on risk:high           (overall risk >= High)
      --fail-on coupling:15.0       (any project coupling_score exceeds threshold)
      --fail-on consumers:20        (consumer count exceeds threshold)
      Multiple --fail-on flags can be combined (all evaluated, first violation triggers exit 1).
      Reports are still produced normally so the developer can see what triggered the failure.
- [x] --output-format json for structured consumption by pipeline scripts (already implemented in json_reporter.py, works across all modes)
- [ ] Configurable thresholds via .scatter.yaml:
      ci:
        fail_on:
          - cycles
          - risk: high
          - coupling: 15.0
          - consumers: 20

PR BLAST RADIUS COMMENTS — GITHUB ✅ COMPLETED (2026-03-22)
- [x] GitHub Action workflow template (tools/github-action/scatter-impact.yml)
      - Zero-install: clones scatter from source, installs via uv (~30s)
      - No marketplace action, no org approvals, no Docker
      - Triggers on PR to main when .cs/.csproj/.sln files change
      - Posts markdown impact report as PR comment via GITHUB_TOKEN
      - Updates existing comment on subsequent pushes (no spam)
      - Optional AI enrichment via GOOGLE_API_KEY env var
      - Pinnable scatter version via SCATTER_REF repo variable
      - Skips draft PRs, cancels in-progress runs for same PR
      - Path filtering, concurrency control, explicit error handling
- [x] Setup README with troubleshooting and advanced config (graph caching)
- [x] mkdocs documentation page (reference/github-action.md)
- [x] CI/CD Integration section in main README
- [ ] Post impact analysis as PR comment (Azure DevOps) — deferred
- [ ] Configurable: suppress comment if blast radius = 0
- [ ] Test against a real .NET repo with active PRs

AUTOMATIC BASELINE UPDATES (MEDIUM PRIORITY)
- [ ] Update baseline automatically on merge to main
- [ ] Store baseline history (keep last N baselines)
- [ ] Trigger via CI/CD pipeline or git hook

COUPLING THRESHOLDS (MEDIUM PRIORITY)
- [ ] Configurable gates in .scatter.yaml:
      max_blast_radius: 10 (warn if more than N projects affected)
      max_coupling_increase: 5% (warn if coupling score increases)
      block_new_cycles: true (fail if PR introduces circular dependency)
- [ ] Output threshold violations in CI/CD check mode

WATCH MODE (TIER 4 — DEFERRED, no user demand)
- [ ] `scatter analyze --watch` — re-run analysis on file changes
- [ ] Use filesystem events (watchdog or similar)
- [ ] Useful for interactive local exploration during development
- [ ] Debounce rapid changes (wait 500ms after last change)

PROGRESS BAR / STREAMING PROGRESS (TIER 4 — DEFERRED, graph cache means most runs <2s)
Ref: OUTPUT_REPORT_EVALUATION.md, Addendum Section G
- [ ] Add tqdm progress bars to stderr for long-running scans
      Scanning projects... 1,247 .csproj files found
      Parsing project files... [=========>          ] 45% (562/1247)
      Building edges... [==================>] 92%
      Done in 47.3s
- [ ] Show progress per phase: file discovery, project mapping, consumer analysis
- [ ] Suppress in --quiet mode and CI/CD check mode

PHASE 5 TESTING
- [ ] Unit tests for exit code logic (threshold evaluation)
- [ ] Unit tests for PR comment formatting (GitHub + Azure DevOps)
- [ ] Integration test: CI/CD check mode with known blast radius
- [ ] Integration test: threshold violations trigger correct exit codes
- [ ] Integration test: watch mode detects file changes and re-runs

============================================================================
INITIATIVE 8: PRODUCTION READINESS — CLI DECOMPOSITION, PACKAGING & CI (v2.0)
✅ PHASES 1-3, 5 COMPLETED — Phase 4 (Docker) remaining
Goal: Make scatter installable, testable in CI, and containerized.
Dependency: None (can start immediately, but Tier 2 CI/CD gates depend on this).
============================================================================

PHASE 1: DECOMPOSE __main__.py MONOLITH ✅ COMPLETED (2026-03-25)
Target: Reduce __main__.py from ~700 lines to ~71 lines (parse args, load config, dispatch).
- [x] Extract mode handlers into scatter/modes/ package:
      - scatter/modes/git.py, target.py, sproc.py (thin wrappers calling cli.py analysis functions)
      - scatter/modes/impact.py, graph.py, dump_index.py (extracted inline handlers)
      - scatter/modes/setup.py (shared setup: logging, path resolution, config, AI provider,
        solution scanning, pipeline CSV, graph context, ModeContext assembly)
      - scatter/modes/__init__.py (package exports)
- [x] CLI argument parser already in scatter/cli_parser.py (build_parser())
- [x] Slim __main__.py to 71 lines: parse args → setup_logging → validate → resolve_paths →
      load_config → setup_ai → scan_solutions → load_batch_jobs → load_pipeline_csv →
      build_graph_context → build_mode_context → dispatch to handler
- [x] Verify `python -m scatter` and `python scatter.py` still work identically
- [x] 30 e2e CLI tests cover all 6 modes across all output formats
- [x] No behavioral changes — 846 tests pass, smoke tests pass, ruff clean
- [x] Leadership review fixes: deduplicated _populate_graph_solutions, fixed repo_path
      strict=True divergence, moved method_name validation, consolidated config loading

PHASE 2: PYTHON PACKAGING WITH UV ✅ COMPLETED (2026-03-21)
Target: `uv run scatter` works. `uv sync` installs everything. No more manual venv management.
Ref: docs/UV_PACKAGING_PLAN.md
- [x] Create pyproject.toml with pytest and coverage config
- [x] Pin dependency versions in requirements.txt (compatible ranges)
- [x] Add missing implicit dependencies (PyYAML, pandas, pytest, pytest-cov)
- [x] Expand pyproject.toml: [build-system] hatchling, [project] metadata,
      dependencies, [project.scripts] entry point, [dependency-groups] dev,
      [tool.hatch.build.targets.wheel] packages = ["scatter"]
- [x] pandas moved to [project.optional-dependencies] pipeline-tools
      (30MB dep used only by parse_pipeline_artifacts.py utility — Fatima, Tomás)
- [x] Generate uv.lock (58 packages resolved, committed for reproducibility)
- [x] Add .python-version (3.11 — development default, requires-python >= 3.10)
- [x] Delete requirements.txt (replaced by pyproject.toml + uv.lock)
- [x] Verify uv sync, uv run scatter, uv run python scatter.py, uv run pytest
- [x] Update CLAUDE.md, getting-started.md, CLAUDE_SKILLS.md, claude-skills ref
- [x] Version consistency test: pyproject.toml matches __version__.py (Anya)
- [x] 789 tests passing, 0 regressions

PHASE 3: CI PIPELINE — COMPLETED (2026-03-21)
Target: GitHub Actions runs tests, linting, and type checking on every push/PR.
- [x] Create .github/workflows/ci.yml with:
      - Install uv via `astral-sh/setup-uv@v4` action with `enable-cache: true`
      - Test job: pytest with coverage (matrix: Python 3.10, 3.11)
      - Lint job: `uv run ruff check scatter/` + `uv run ruff format --check scatter/`
      - Type-check job: `uv run mypy scatter --ignore-missing-imports`
      - Trigger: push to main + all PRs
      - Three parallel jobs (test, lint, type-check)
- [x] Configure ruff baseline (pyproject.toml [tool.ruff]):
      - target-version py310, line-length 100
      - select E/F/W, ignore E501 (long strings) + E741 (ambiguous names)
      - __init__.py F401 per-file ignore (barrel re-exports)
      - Auto-fixed 113 unused imports, 20 f-string, 3 unused vars
      - Formatted 44 files
- [x] Configure mypy baseline (pyproject.toml [tool.mypy]) — 0 errors
- [x] All 789 tests pass in CI (verified on 3.10 and 3.11)
- [x] .git-blame-ignore-revs for formatting commit (GitHub respects automatically)
- [x] Developer experience scripts:
      - tools/setup.sh: one-time bootstrap (python check, uv check, deps, git config, skills)
      - tools/check.sh: local CI mirror (--quick for lint-only ~2s)
- [ ] Badge in README for CI status
- [ ] Create conftest.py with shared fixtures (deferred — not blocking)

PHASE 4: DOCKER SUPPORT (MEDIUM PRIORITY)
Target: `docker build -t scatter .` and run against mounted repos.
- [ ] Create Dockerfile:
      - Multi-stage build (builder + runtime)
      - Based on python:3.12-slim
      - Install uv in builder stage (COPY --from=ghcr.io/astral-sh/uv:latest)
      - `uv sync --frozen --no-dev` for production deps only
      - Default entrypoint: scatter (via uv run or direct venv path)
      - Mount point for repo-under-analysis at /workspace
      - Git installed in runtime image (needed for git analysis + cache invalidation)
- [ ] Create .dockerignore (exclude .git, __pycache__, .venv, test data, benchmarks, docs)
- [ ] Verify: docker run -v /path/to/repo:/workspace scatter --target-project ... works
- [ ] Add Docker usage example to README

PHASE 5: INITIATIVE 8 TESTING ✅ COMPLETED (2026-03-25)
- [x] Integration test: `scatter` CLI entry point invoked via subprocess (30 e2e tests)
- [x] Integration test: each command module independently callable (all 6 modes tested)
- [x] Test: `uv run scatter --version` works after fresh `uv sync`
- [ ] Test: Docker build succeeds (can be manual or CI) — blocked on Phase 4

============================================================================
INITIATIVE 11: SOW DIFFERENTIATOR (v2.1 — HIGH PRIORITY)
Goal: Polish the SOW/impact analysis feature into an evaluator-ready artifact.
Ref: docs/SOW_DIFFERENTIATOR_PLAN.md, docs/LEADERSHIP_DESIGN_REVIEW.md
Dependency: CI pipeline (shipped), codebase index (shipped, Initiative 10)
============================================================================

PHASE 1: E2E TESTS + MARKDOWN POLISH ✅ COMPLETED (2026-03-22)
Target: Prove the SOW pipeline works end-to-end, make the report shareable.
- [x] 22 e2e tests (test_impact_e2e.py) against real sample .NET projects
      - 7 core pipeline, 5 output formats, 4 resilience, 6 error handling
      - Mock AI provider with prompt-based dispatch (all tasks use model.generate_content)
      - Tests assert new markdown structure from day one (tests-as-spec, Priya)
      - Includes: no-AI tracing (Marcus), determinism (Anya), partial AI support (Fatima),
        large SOW text (Fatima)
- [x] Restructured markdown report:
      - Summary section with narrative before stats table (Sam)
      - Metric | Value table (risk, complexity, consumer counts, pipelines, confidence)
      - Target confidence + evidence under each target heading
      - Section renames: Consumer Detail → Affected Projects (Sam)
      - Template-driven Next Steps footer with observational wording (Fatima, Sam)
      - Pipeline/Solutions columns suppressed when no data exists
- [x] Fixed direct consumer propagation_parent bug (BatchProcessor was nested under
      WebPortal despite being a direct consumer — depth 0 consumers no longer inherit parent)
- [x] Demo script: tools/demo-impact.sh (works with or without API key)
- [x] Updated existing markdown tests for new structure (3 assertions)
- [x] 811 tests passing at time of completion, all checks green (now 846)

PHASE 2: REPORT POLISH FOLLOW-UPS (MEDIUM PRIORITY — cosmetic, not blocking)
- [ ] Show risk once at target level instead of broadcasting identical per-target
      risk text to every consumer in the tree. Currently all consumers show the
      same justification because risk is assessed per-target, not per-consumer.
- [ ] Per-consumer coupling narratives. Currently the tree shows identical coupling
      text for all direct consumers when using mock AI. In production with real AI
      each consumer gets a unique narrative, but the demo output should demonstrate
      this with varied mock responses or a note in the docs.
- [ ] Evaluate whether Affected Projects table adds value alongside the tree.
      Tree already shows consumer name, confidence, depth, via, risk, coupling.
      Table duplicates most of this. Consider: suppress table when <N consumers,
      or only show table when --graph-metrics adds columns the tree doesn't have.

PHASE 3: EVALUATOR EXPERIENCE (MEDIUM PRIORITY)
- [ ] Worked example in docs: walk through a real SOW → impact report, explain
      each section. Hand this to evaluators.
- [ ] Harden SOW failure modes (Fatima):
      - Vague SOW → clear "no targets identified" with suggestions
      - No API key → single-line actionable message
      - Rate limit / API error → graceful degradation
- [ ] Demo script in CI as a smoke test (Kai) — proves SOW pipeline can't silently break

============================================================================
PIPELINE REGISTRY (DESIGN COMPLETE — BLOCKED ON DEVOPS REPO ACCESS)
Ref: docs/PIPELINE_REGISTRY_DESIGN.md
- [ ] Phase 1: Module structure + canonical format + Azure DevOps parser
- [ ] Phase 2: Wire into scatter core (Dict[str,str] → PipelineRegistry)
- [ ] Phase 3: Resolver + staleness
- [ ] Phase 4: Additional parsers (as needed)
============================================================================

============================================================================
DEMO RELEASE CANDIDATE — GAP ITEMS (2026-03-28)
Scatter is shippable for team evaluation. These are the gaps that could
matter during a demo or hands-on eval. Items are cross-references to
existing tasks above — not new work, just prioritization for the demo.
============================================================================

BLOCKING (evaluator cannot complete workflow without these):
  (none — all 5 modes, 6 output formats, AI features, and CLI work end-to-end)

HIGH IMPACT (evaluator will notice, affects credibility):
- [ ] Test against a real .NET repo with active PRs (Initiative 7, line ~974)
      Without this, we're shipping on synthetic data confidence only.
- [ ] Worked example in docs: walk through a real SOW → impact report,
      explain each section (Initiative 11 Phase 3, line ~1142)
      Evaluators need a guided path, not just --help.
- [ ] Docker support (Initiative 8 Phase 4, line ~1081)
      Evaluator without Python/uv installed hits a wall at step 1.

MEDIUM IMPACT (polish, evaluator may notice):
- [ ] --fail-on exit codes (Initiative 7, line ~942)
      Without this, scatter is a read-only tool — can't enforce gates in CI.
- [ ] Risk text duplicated across consumers in impact report
      (Initiative 11 Phase 2, line ~1128)
      Same justification on every consumer looks like a bug.
- [ ] Per-consumer coupling narratives show identical text with mock AI
      (Initiative 11 Phase 2, line ~1131)
      Demo output looks repetitive.

LOW IMPACT (nice-to-have, unlikely to block eval):
- [ ] Evaluate whether Affected Projects table adds value alongside tree
      (Initiative 11 Phase 2, line ~1135)
- [ ] Harden SOW failure modes — vague SOW, no API key, rate limit
      (Initiative 11 Phase 3, line ~1143)
- [ ] Demo script in CI as smoke test (Initiative 11 Phase 3, line ~1147)

============================================================================
PROJECT STATUS SUMMARY (VALIDATED AGAINST CODEBASE 2026-03-30)
============================================================================

CODEBASE STATE (updated 2026-03-28, post console reporter redesign + truncation removal):
  - scatter/ package with 29+ modules across core/, scanners/, analyzers/, ai/, reports/, compat/, store/, modes/
  - scatter/core/tree.py: shared tree-building utilities (build_adjacency, CONFIDENCE_LABEL_RANK)
  - scatter.py: 10-line thin entry point (delegates to scatter/__main__.py via runpy)
  - scatter/__main__.py: 71 lines (parse args → shared setup → mode dispatch)
  - scatter/modes/: 8 files — mode handlers (git, target, sproc, impact, graph, dump_index) + setup.py + __init__.py
  - scatter/cli.py: ModeContext, ModeResult, dispatch_legacy_output, analysis functions, graph enrichment helpers
  - tools/setup.sh + tools/check.sh: developer bootstrap and local CI mirror
  - tools/demo-impact.sh: one-command SOW impact demo for evaluators
  - tools/github-action/: GitHub Action workflow template for PR impact comments
  - .github/workflows/ci.yml: GitHub Actions CI (test 3.10/3.11, ruff, mypy, smoke tests)
  - .git-blame-ignore-revs: formatting commit excluded from blame
  - scatter/config.py: YAML config system with layered precedence (AIConfig, GraphConfig, DbConfig)
  - scatter/ai/router.py: AI task router with lazy provider creation and caching
  - scatter/_legacy.py: ~150 lines (backward-compat AI/Gemini wrappers)
  - scatter/scanners/db_scanner.py: DB dependency detection (sprocs, DbSet, DbContext, SQL, conn strings)
  - scatter/analyzers/domain_analyzer.py: domain boundary detection + extraction feasibility
  - scatter/analyzers/health_analyzer.py: health dashboard + deterministic observation rules
  - scatter/reports/graph_reporter.py: graph console + JSON + CSV + Mermaid output
  - scatter/reports/markdown_reporter.py: markdown output for all 3 modes (build/write separation, stdout fallback)
  - scatter/reports/console_reporter.py: render_tree() public API (used by markdown_reporter.py)
  - tools/generate_synthetic_codebase.py: synthetic .NET codebase generator (100-800 project presets)
  - tools/benchmark_graph_build.py: graph builder benchmark harness with per-stage timing
  - docs/ADR_GRAPH_PERFORMANCE.md: architecture decision record for graph builder 100x optimization
  - 940 tests across 35 test files (939 pass, 1 xfail)
  - 30 e2e CLI tests (tests/integration/test_cli_e2e.py) covering all 6 modes × all output formats
  - 22 AI budget tests (tests/unit/test_ai_budget.py) — budget tracking, proxy, retries, CLI, config
  - CI: ruff check + ruff format + mypy (0 errors) + pytest + smoke tests in GitHub Actions
  - Benchmarks exist for Phase 1 file discovery, Phase 2.1 content analysis, and graph builder at scale
  - AI integration: Google Gemini via AIProvider protocol + AIRouter + AIBudget rate limiting
  - scatter/ai/budget.py: AIBudget call tracking, RateLimitedModel proxy, exponential backoff + jitter
  - Config: .scatter.yaml (repo) + ~/.scatter/config.yaml (user) + env vars + CLI
  - Output formats: console, CSV, JSON, markdown (+ Mermaid for graph mode)
  - Dependency graph: persistent, cached, with coupling metrics + cycle detection + domain clusters + DB deps
  - Health dashboard: deterministic observations (stable_core, high_coupling, in_cycle, low_cohesion_cluster, db_hotspot)
  - Type extraction: supports readonly/ref struct modifiers, record variants, delegates, primary constructors

COMPLETED (v1.x):
  Initiative 1 — Multiprocessing Overhaul:
  ✅ Phase 1: File discovery parallelization (find_files_with_pattern_parallel)
  ✅ Phase 1 benchmarking + critical perf fix (benchmark_results/phase1_*)
  ✅ Phase 2.1: Content analysis parallelization (analyze_cs_files_parallel)
  ✅ Phase 2.1 benchmarking (benchmark_results/phase21_*)
  ✅ Phase 2.2: XML parsing parallelization (parse_csproj_files_parallel)
  ✅ Phase 2.3: Project mapping parallelization (map_cs_to_projects_parallel)
  ✅ Configuration: --disable-multiprocessing, --max-workers, --chunk-size,
     --cs-analysis-chunk-size, --csproj-analysis-chunk-size
  ✅ Testing: 514 tests (513 pass, 1 xfail) across 17 files (7 phase1 + 24 phase2.3 + 7 hybrid git + 82 impact + 24 config + 46 graph + 25 coupling + 28 cache + 36 db_scanner + 15 domain + 21 reporters + 24 report_quality + 21 filter_pipeline + 48 type_extraction + 14 find_enclosing_type + 54 new_samples + 39 markdown_reporter)
  ✅ Adaptive worker scaling via estimate_file_count()
  ✅ Benchmarking infrastructure (benchmark_framework.py, benchmark_analysis.py)
  ✅ Scalability test data (20-project and 100-project synthetic codebases)

COMPLETED (v1.x — continued):
  Initiative 1 — Multiprocessing Overhaul:
  ✅ Benchmarking Phase 2.2/2.3 (benchmark_phase22_23.py — correctness verified)
  ✅ Phase 3: Method filtering parallelized (step 5 now uses analyze_cs_files_parallel)
  ✅ Phase 4: All content analysis parallelized, profiled, no bottlenecks
  ✅ End-to-end profiling: find_consumers() pipeline fully validated
  🔜 Phase 5: Adaptive scaling done. Pool reuse + memory optimization deferred to v2.0.

COMPLETED (v1.x — continued):
  Initiative 2 — Hybrid Git Analysis:
  ✅ get_diff_for_file() and get_affected_symbols_from_diff() functions
  ✅ --enable-hybrid-git CLI flag (opt-in, reuses existing Gemini config)
  ✅ Integrated into Step 2 type extraction loop with fallback to regex
  ✅ 7 acceptance tests in test_hybrid_git.py (70 total tests passing)

COMPLETED (v2.0):
  Initiative 3 — Modularization & AI Backend Foundation:
  ✅ Phase 1: Package skeleton + core infrastructure (scatter/core/)
  ✅ Phase 2: Scanner + Analyzer extraction (scanners/, analyzers/, compat/)
  ✅ Phase 3: Git analyzer + reporters + CLI entry point (__main__.py, reports/)
  ✅ Phase 4: AI Provider protocol + Gemini migration (ai/base.py, gemini_provider.py)
  ✅ Phase 5: Configuration system + AI router (config.py, ai/router.py)
  Initiative 4 — CSE Impact Analysis & AI Enrichment:
  ✅ All tasks completed (53 tests, see Initiative 4 section above)

COMPLETED (v2.0 — continued):
  Initiative 5 — Dependency Graph, Metrics & Domain Analysis (v2.0 Phase 3):
  ✅ Phase 1: Graph model + single-pass O(P+F) builder (46 tests)
  ✅ Phase 2: Coupling metrics + Tarjan's SCC cycle detection (25 tests)
  ✅ Phase 3: Graph persistence + smart git cache invalidation + --graph CLI (28 tests)
  ✅ Phase 4: DB dependency scanner + sproc_shared edges + --include-db CLI (36 tests)
  ✅ Phase 5: Domain boundary detection + extraction feasibility scoring (15 tests)
  ✅ Phase 6: Graph reporters + health dashboard + Mermaid + CSV export (21 tests)
  Initiative 6 — Reporting & Extraction Planning (v2.0 Phase 4):
  ✅ Phase 1: Report quality fixes (24 tests)
  ✅ Phase 2: Filter pipeline visibility (21 tests)
  ✅ Phase 3: Blast radius tree view (20 tests) — propagation_parent tracking, tree rendering,
     shared tree.py builder, orphan re-parenting, JSON propagation_tree, CSV PropagationParent column.
  ✅ Phase 4: Markdown output format (39 tests) — build/write separation (build_*() → str, write_*()
     thin wrappers), stdout fallback, _require_output_file() helper, _md_table with row padding,
     render_tree() reuse for blast radius trees, generate_mermaid() for dependency diagrams,
     cell escaping, consistent metadata footers, proper type annotations.
  ✅ Test quality cleanup: deleted 1 zombie file (test_phase21_overhead.py), fixed 5 ghost tests,
     strengthened 3 dead-end assertions, removed 8 redundant tests from test_multiprocessing_phase1.py.
     Net: 464 → 456 → 476 → 514 → 539 tests across 19 files (538 pass, 1 xfail).
  ✅ --summarize-consumers wiring (7 tests) — connected AI summarization to all 3 legacy modes,
     shared prompt template in scatter/ai/base.py, per-file progress logging, stem collision fix.
  ✅ --graph-metrics enrichment (17 tests) — GraphContext dataclass, post-processing enrichment
     for all modes (git/target/sproc/impact), schema stability (reporters include graph columns
     when flag present), graceful degradation on graph failure.

IN PROGRESS (v2.0 — reprioritized 2026-03-14):
  Tier 1 — Credibility & Adoption (4 of 4 shipped):
    - ✅ Fix --summarize-consumers wiring
    - ✅ Integrate dependency graph into analysis modes (--graph-metrics)
    - ✅ Claude Code skills in tools/claude-skills/ (5 skills + setup + smoke test)
    - ✅ Deployment checklist / scatter pipelines shortcut
  Tier 1.5 — Incremental Graph Updates (prerequisite for Tier 2 CI/CD):
    - Per-file facts cache + lazy git-diff patching (~3-4 days)
    - Ref: docs/INCREMENTAL_GRAPH_PLAN.md
  Tier 2 — CI/CD Governance (makes scatter sticky infrastructure):
    - Initiative 7: CI/CD exit codes (--fail-on)
    - Initiative 7: PR blast radius comments (GitHub/ADO)
  Tier 3 — Strategic Features (baselines, extraction planning, HTML)
  Tier 4 — Deferred (unified report model, watch mode, extra AI providers)

NOT STARTED (v2.0):
  Initiative 7 — CI/CD Integration & Workflow (v2.0 Phase 5)
  Initiative 8 Phase 4 — Docker support (only remaining Phase)

RECOMMENDED EXECUTION ORDER (reprioritized 2026-03-13):

  Rationale: Previous order followed architectural dependency (Phase 5 before
  Phase 6, etc.). New order follows adoption-driven sequencing: fix credibility
  issues first, drive adoption second, make scatter sticky third. User feedback
  from real usage should inform later priorities. A tool nobody uses has zero
  value regardless of engineering quality.

  COMPLETED:
  1. ✅ DONE — Initiative 1 multiprocessing work complete
  2. ✅ DONE — Initiative 2 Hybrid Git Analysis complete
  3. ✅ DONE — Initiative 3: Modularization (all 5 phases complete)
  4. ✅ DONE — Initiative 4: CSE Impact Analysis & AI Enrichment complete (53 new tests)
  5. ✅ DONE — Initiative 5: Dependency graph — all 6 phases complete (171 tests)
  6. ✅ DONE — Initiative 6 Phase 1: Report quality fixes (24 tests)
  7. ✅ DONE — Initiative 6 Phase 2: Filter pipeline visibility (21 tests)
  8. ✅ DONE — Initiative 6 Phase 3: Blast radius tree view (20 tests)
  9. ✅ DONE — Initiative 6 Phase 4: Markdown output (39 tests)

  TIER 1: CREDIBILITY & ADOPTION (highest ROI, lowest effort)
  10. ✅ DONE — Fix --summarize-consumers wiring (7 tests)
  11. ✅ DONE — Integrate dependency graph into analysis modes via --graph-metrics (17 tests)
  12. ✅ DONE — Claude Code skills in tools/claude-skills/ (5 skills, setup script,
      smoke test, docs/CLAUDE_SKILLS.md. Team review: shell injection fix, auto-pick
      scoping, error handling, trigger phrase expansion)
  13. ✅ DONE — Deployment checklist / `--output-format pipelines` (14 tests)

  TIER 1.5: INCREMENTAL GRAPH UPDATES (prerequisite for CI/CD — 3-4 days)
  14. ✅ DONE — Incremental graph updates — per-file facts cache, lazy git-diff patching,
      content hash early cutoff, project-level edge attribution, property-based testing.
      Ref: docs/INCREMENTAL_GRAPH_PLAN.md. 49 tests.

  TIER 1.75: TRANSPARENT GRAPH ✅ ALL PHASES COMPLETE (makes graph the default fast path)
  Ref: docs/ADR_TRANSPARENT_GRAPH.md
  15. Phase A — Auto graph loading ✅ DONE
      Auto-load graph when cache exists (no --graph-metrics flag needed).
      Add --no-graph escape hatch. "graph_enriched" metadata field. 15 tests.
      Ref: docs/TRANSPARENT_GRAPH_PHASE_A_PLAN.md
  16. Phase B — Graph-accelerated consumer lookup ✅ DONE
      Use graph reverse lookup to replace stages 1-2 of find_consumers().
      Graph becomes the primary lookup engine, not a bolt-on enrichment layer.
      Stages 3-5 (namespace/class/method) still run on graph consumer set.
      Target-not-in-graph fallback to filesystem. FilterStage.source field. 14 tests.
      Ref: docs/TRANSPARENT_GRAPH_PHASE_B_PLAN.md
  16a. DEFERRED: --verify-graph diagnostic flag
      Runs both graph and filesystem paths, logs divergence. Deferred from
      Phase B — parametrized integration tests provide correctness verification.
      Add if divergence is reported in practice.
  17. Phase C — First-run graph build ✅ DONE
      Build graph on first analysis run via _ensure_graph_context() idempotent
      helper at all 4 enrichment sites (branch, target, sproc, impact). Enrich
      results on very first run. --no-graph skips build. 7 tests.
      Ref: docs/TRANSPARENT_GRAPH_PHASE_C_PLAN.md

  TIER 1.9: PRODUCTION READINESS (prerequisite for CI/CD governance)
  18. ✅ DONE — Initiative 8 Phase 1: Decompose __main__.py monolith (2026-03-25)
      700-line main() → 71 lines. scatter/modes/ package with 6 mode handlers +
      setup.py. 30 e2e CLI tests. Leadership review fixes applied.
  19. ✅ DONE — Initiative 8 Phase 2: Python packaging with uv (2026-03-21)
      pyproject.toml + uv.lock, hatchling build backend, `uv run scatter`,
      pandas to optional deps, version consistency test. 789 tests.
  20. ✅ DONE — Initiative 8 Phase 3: CI pipeline (2026-03-21)
      GitHub Actions: pytest 3.10/3.11 matrix, ruff check+format, mypy,
      smoke tests. .git-blame-ignore-revs. tools/setup.sh + tools/check.sh.
  20a. ✅ DONE — Initiative 7 Phase 5: AI call safety (2026-03-26)
      AIBudget + RateLimitedModel proxy, --max-ai-calls, exponential backoff,
      22 tests. PR #11 merged. Mypy fully clean. 878 total tests.
  21. Initiative 8 Phase 4: Docker support — multi-stage Dockerfile, .dockerignore,
      mount /workspace for repo-under-analysis

  TIER 1.9b: SOLUTION-AWARE GRAPH + SOW INDEX (analysis quality for real monoliths)
  22. ✅ DONE — Initiative 9: Solution-aware graph — all 5 phases (2026-03-20)
      64 new tests. Solution scanner, ProjectNode.solutions, cross-solution coupling
      metrics (SolutionMetrics, bridge projects), solution alignment in clusters,
      6 e2e validation tests. Team decisions: no label propagation affinity (Tomás),
      single-pass O(E) algorithm (Marcus), post-hoc alignment (Devon).
  22b. Initiative 10: SOW codebase index — 4 phases: ✅ SHIPPED 2026-03-19
      Phase 1: Build compact index from graph (names, types, sprocs)
      Phase 2: Rewrite SOW prompt with index as ground truth context
      Phase 3: Ambiguity detection, match evidence, --sow-min-confidence filter
      Phase 4: Wire into pipeline, --dump-index debug flag
      Phase 5: Remove index truncation (2026-03-28) — deleted MAX_INDEX_SIZE cap
        and type-per-project truncation. Full index (~120K tokens for 800 projects)
        fits within modern LLM context windows. Context-too-large errors now surface
        actionable messages. 3 new error-handling tests.
      Ref: docs/SOW_CODEBASE_INDEX_PLAN.md

  TIER 2: CI/CD GOVERNANCE (makes scatter infrastructure, not optional)
  23. Initiative 7: CI/CD exit codes --fail-on (HIGH — turns scatter into an
      architecture governance gate; cycles, risk:high, coupling:N, consumers:N)
  24. Initiative 7: PR blast radius comments GitHub/ADO (HIGH — visible to reviewers
      without anyone remembering to run scatter; combined with skills = two always-on
      touchpoints)

  TIER 3: STRATEGIC FEATURES (do after adoption is established)
  25. Baselines + diff reports (Initiative 6 Phase 6 — needs real usage data first;
      without baseline history from actual users, diff engine is built against
      synthetic data)
  26. Extraction planning mode (HIGH strategic value but needs graph integration
      from #11 and real-world validation of coupling metrics)
  27. HTML reports (polish — markdown already covers "paste into ticket" workflow;
      vis-network.js pattern from CODEGRAPHCONTEXT_COMPARISON is good reference)

  TIER 4: DEFERRED (build when concrete demand materializes)
  28. Initiative 6 Phase 5: Unified AnalysisReport data model (internal refactor
      no user sees; do when a 5th output format is needed or schema inconsistency
      blocks something concrete)
  29. Watch mode (graph cache makes repeat runs <2s; no user has asked for this)
  30. Additional AI providers — Anthropic, OpenAI, Azure, Ollama (Gemini works;
      build WEX AI Platform provider when it exists; others are speculative)
  31. AI response caching + token budget manager (premature optimization; AI calls
      are infrequent and cheap)
  32. MCP server (per CLAUDE_INTEGRATION_EVALUATION: skills first, MCP when
      cross-client demand materializes)
  33. Streaming progress / tqdm (nice DX but graph cache means most runs <2s)

KEY FINDINGS:
  1. All 5 steps of find_consumers() are now parallelized (steps 2-5 use
     ProcessPoolExecutor, step 1 uses find_files_with_pattern_parallel).
  2. Correctness: sequential vs parallel results match 100% across all scenarios.
  3. At test scale (50-300 projects), sequential is faster due to ProcessPoolExecutor
     startup overhead (~400ms/pool). Parallelization benefits appear at enterprise
     scale (10,000+ projects) where per-file I/O dominates.
  4. No bottlenecks found — time scales linearly with file count.
  5. Phase 5 optimization (pool reuse, memory) deferred to v2.0 modularization
     where it can be done properly with a shared pool across the module.
  6. Initiatives 3 & 4 fully complete — config system, AI router, impact analysis all shipped.
  7. 878 tests across 33 files (878 pass, 1 xfail, 5 subtests).
  8. Initiative 5 all 6 phases complete (171 tests) — graph model, coupling metrics, cycle detection,
     graph persistence, DB dependency mapping, domain boundary detection, health dashboard, Mermaid + CSV.
  9. Initiative 6 Phase 1 complete (24 tests) — JSON serialization fixes, metadata, console polish, CSV cleanup.
  10. Initiative 6 Phase 2 complete (21 tests) — FilterStage/FilterPipeline dataclasses, find_consumers()
      returns pipeline, arrow-chain in console/JSON/CSV, diagnostic hints for zero-result stages.
  11. Initiative 6 Phase 3 complete (20 tests) — propagation_parent in EnrichedConsumer, BFS parent tracking,
      shared tree.py builder (build_adjacency + CONFIDENCE_LABEL_RANK), orphan re-parenting, box-drawing
      console tree, JSON propagation_tree, CSV PropagationParent column.
  12. Initiative 6 Phase 4 complete (39 tests) — markdown output for all 3 modes with build/write separation,
      stdout fallback, _require_output_file() helper, cell escaping, render_tree() reuse, Mermaid diagrams,
      row padding, consistent metadata footers, column-count validation tests, CLI dispatch tests.
  13. Remaining work reprioritized (2026-03-14): Tier 1 adoption fixes (summarize wiring,
      graph integration, Claude skills, pipeline shortcut) → Tier 2 CI/CD gates → Tier 3
      strategic features → Tier 4 deferred (unified report model, watch mode, extra providers).
  14. Graph builder performance: inverted index optimization achieved 101x speedup at 100 projects
      (172s → 1.7s). 250 projects builds in 10.1s (was killed after 10+ min). 500 projects in 42.8s.
  15. Three remaining bottlenecks at 500-project scale: comment stripping (18s, char-by-char Python loop),
      label propagation (21s, linear scan in get_edges_between), type extraction (14s, sequential I/O).
  16. tracemalloc inflates timing 5-10x for allocation-heavy code — always verify with clean timing runs.
  17. --summarize-consumers wiring fixed (2026-03-14): connected AI summarization to all 3 legacy
      modes, shared SUMMARIZATION_PROMPT_TEMPLATE in scatter/ai/base.py, 7 tests.
  18. --graph-metrics enrichment shipped (2026-03-14): GraphContext in graph_enrichment.py,
      post-processing enrichment for all modes, schema stability in all reporters, 17 tests.
  19. Incremental graph updates planned (2026-03-14): team brainstorm produced 4-phase plan.
      Per-file facts cache + lazy git-diff patching + content hash early cutoff + property-based
      testing. Target: <1s for typical PRs (vs 60s full rebuild). Tier 1.5 prerequisite for CI/CD.
      Ref: docs/INCREMENTAL_GRAPH_BRAINSTORM.md, docs/INCREMENTAL_GRAPH_PLAN.md.
  20. --output-format pipelines shipped (2026-03-15): pipe-friendly output for release managers.
      Sorted unique pipeline names, one per line. Works in legacy + impact modes. Early rejection
      in graph mode via parser.error(). Warns on stderr without --pipeline-csv. 14 tests.
      601 total tests (600 pass, 1 xfail).
  21. Transparent Graph Phase B shipped (2026-03-16): graph-accelerated consumer lookup
      replaces stages 1-2 of find_consumers() with O(1) reverse lookup. Target-not-in-graph
      fallback to filesystem. FilterStage.source field with [graph] annotation. 14 tests.
  22. Transparent Graph Phase C shipped (2026-03-16): first-run graph build via
      _ensure_graph_context() idempotent helper at all 4 enrichment sites. Graph built and
      cached on first analysis run, results enriched immediately. --no-graph escape hatch.
      7 tests. 637 total tests (all pass, 1 xfail). All 3 transparent graph phases complete.
  23. Leadership design review completed (2026-03-17): Priya (Architect) + Marcus (Principal)
      reviewed full codebase. Key findings: untyped result dicts, __init__.py over-exports,
      double file read in graph builder, no E2E CLI tests, no AI call budget cap, hardcoded
      pipeline names, graph._nodes accessed directly bypassing public API.
      Ref: docs/LEADERSHIP_DESIGN_REVIEW.md
  24. __main__.py decomposition merged (2026-03-17): cli.py (766 lines), cli_parser.py
      (197 lines), conftest.py, 3 new test files (test_cli_parser, test_cli_modes,
      test_cli_dispatch). __main__.py reduced from 790 to 67 lines.
      683 total tests (all pass, 1 xfail).
  25. AI rate limiting shipped (2026-03-26): AIBudget + RateLimitedModel proxy in
      scatter/ai/budget.py. --max-ai-calls CLI flag, .scatter.yaml config,
      SCATTER_MAX_AI_CALLS env var. Exponential backoff with jitter on transient
      API errors (3 retries, 1-30s delays). BudgetExhaustedError caught by existing
      task module handlers. Zero task module changes — proxy intercepts at model level.
      22 new tests. AI smoke test in tools/check.sh. PR #11 merged.
  26. Mypy fully clean (2026-03-26): 5 errors fixed across budget.py,
      gemini_provider.py, markdown_reporter.py. CI now gates on mypy --strict.
      878 total tests (all pass, 1 xfail).

============================================================================
INITIATIVE 7: STRUCTURAL IMPROVEMENTS (Leadership Review Findings)
Ref: docs/LEADERSHIP_DESIGN_REVIEW.md (2026-03-17)
Priority: Address during or immediately after feature/decompose-main
============================================================================

PHASE 1: Type Safety & API Surface (HIGH PRIORITY — highest leverage)
----------------------------------------------------------------------
- [ ] Create ConsumerResult dataclass to replace Dict[str, Union[str, Dict, List[str]]]
      Replace all string-key dict construction in v1_bridge._process_consumer_summaries_and_append_results
      Update all 7 reporters to consume the typed dataclass instead of dict key lookups
- [ ] Fix lowercase `any` type hints in parallel.py (lines 295, 381-385) → typing.Any
- [ ] Add __contains__ method to DependencyGraph (or has_node(name) -> bool)
      Replace all graph._nodes direct access in graph_builder.py (6 callsites)
- [ ] Extract hardcoded "cdh-batchprocesses-az-cd" from v1_bridge.py into config

PHASE 2: CLI Subcommands (HIGH PRIORITY — enables independent mode testing)
----------------------------------------------------------------------
- [ ] Introduce argparse subcommands: scatter git, scatter target, scatter sproc,
      scatter impact, scatter graph
- [ ] Extract shared setup (config, AI provider, solution cache, pipeline CSV) into
      a common setup() function
- [ ] Each subcommand gets its own handler — eliminate if/elif chain in __main__.py
- [ ] Move impact mode and graph mode output dispatch into their respective handlers
      (currently inline in __main__.py)

PHASE 3: Graph Builder Performance (MEDIUM PRIORITY)
----------------------------------------------------------------------
- [ ] Eliminate double file read: cache extracted identifiers (dict[Path, set[str]])
      during step 4, reuse in step 5c type_usage edge construction
- [ ] Drop indent=2 from graph cache JSON serialization (graph_cache.py save_graph)
- [ ] Evaluate compact serialization (msgpack or non-indented JSON) for large graphs
- [ ] Fix parallel file discovery overlapping directory dedup — filter child dirs
      before chunking instead of deduping results after

PHASE 4: Test Infrastructure (MEDIUM PRIORITY — refactoring safety net)
----------------------------------------------------------------------
- [ ] Add end-to-end CLI tests: invoke main() with real args against sample .NET
      projects, capture stdout/stderr, assert output format correctness
- [ ] Add equivalence test: graph builder and consumer analyzer produce same consumer
      set for known inputs (shared fixture validates no drift)
- [ ] Move test files into tests/ directory (tests/unit/, tests/integration/)
- [ ] Wire benchmark suite into CI as a non-blocking performance regression check

PHASE 5: AI Call Safety ✅ COMPLETED (2026-03-26)
----------------------------------------------------------------------
Ref: AI CALL BUDGET & RATE LIMITING section above (Initiative 3 tier list)
- [x] Add --max-ai-calls flag to cap AI API calls (AIBudget + CLI + config + env var)
- [x] Budget summary logged at INFO after mode dispatch completes
- [x] Add exponential backoff on AI call failures (RateLimitedModel proxy, 3 retries, 1-30s)
- [x] BudgetExhaustedError caught by task modules — no task module changes needed
Note: Per-call timeout deferred — Gemini SDK manages its own timeouts.
      "Log total planned API calls" deferred — not needed with budget cap approach.

PHASE 6: Cleanup (LOW PRIORITY — do when convenient)
----------------------------------------------------------------------
- [ ] Gut __init__.py re-exports: move to explicit imports everywhere
      Create scatter.api module with stable public surface if needed
- [ ] Delete _legacy.py wrappers (configure_gemini, gemini_model, etc.)
- [ ] Remove types.SimpleNamespace shim in _ensure_graph_context — make
      build_graph_context accept explicit parameters
- [ ] Converge consumer_analyzer and graph_builder on shared ReferenceResolver
      for .csproj ProjectReference resolution

============================================================================
INITIATIVE 9: SOLUTION-AWARE GRAPH INTEGRATION ✅ ALL 5 PHASES COMPLETED (2026-03-20)
Goal: Make the dependency graph aware of .sln solution files — use solution
membership as a signal for clustering, coupling metrics, and health analysis.
Ref: docs/SOLUTION_AWARE_GRAPH_PLAN.md, docs/INITIATIVE_9_PHASE{1-5}_PLAN.md
Branch: feature/solution-aware-graph (merged to main)
64 new tests, 788 total passing, 0 regressions.
============================================================================

PHASE 1: PARSE SOLUTIONS PROPERLY ✅ COMPLETED
----------------------------------------------------------------------
- [x] Create scatter/scanners/solution_scanner.py
      SolutionInfo dataclass (path, name, project_entries, project_paths as resolved Paths)
      parse_solution_file() — regex parser with C# GUID whitelist (FAE04EC0, 9A19103F)
      scan_solutions() — discover + parse all .sln files in search scope
      build_project_to_solutions() — reverse index: project_name -> List[SolutionInfo]
      BOM tolerance (utf-8-sig), deduplication, format detection, stem collision warnings
- [x] Migrate v1_bridge.find_solutions_for_project() with optional solution_index param
- [x] 21 tests in test_solution_scanner.py (parsing, GUID filtering, path resolution,
      edge cases, reverse index, migration, false-positive prevention)

PHASE 2: SOLUTION MEMBERSHIP IN THE GRAPH ✅ COMPLETED
----------------------------------------------------------------------
- [x] Add solutions: List[str] field to ProjectNode (default empty list, no cache bump)
- [x] to_dict()/from_dict() serialize solutions (old caches gracefully default to [])
- [x] Post-process population via _populate_graph_solutions() after graph build
      (not in graph builder — Tomás: builder shouldn't know about solutions)
- [x] scan_solutions() replaces raw find_files_with_pattern_parallel('*.sln') in __main__.py
      Single scan, solution_index threaded through ModeContext, v1_bridge, impact_analyzer,
      graph_enrichment
- [x] CSV: Solutions column (semicolon-delimited). JSON: solutions in metrics + topology
- [x] Console: "Solutions: N" in summary (suppressed when zero)
- [x] 13 tests (model, serialization, integration, reporters)

PHASE 3: CROSS-SOLUTION COUPLING METRICS ✅ COMPLETED
----------------------------------------------------------------------
- [x] SolutionMetrics dataclass in coupling_analyzer.py
      name, project_count, internal_edges, external_edges, cross_solution_ratio,
      incoming_solutions, outgoing_solutions
- [x] compute_solution_metrics(graph) -> (Dict[str, SolutionMetrics], List[str] bridge_projects)
      Single-pass O(E) algorithm (Marcus) with set intersection per edge
      Bridge projects computed globally, not per-solution (Devon)
- [x] Health observations: high_cross_solution_coupling (warning, ratio > 0.5),
      solution_bridge_project (info, 3+ solutions AND fan_in >= 5)
- [x] Console: Solution Coupling table (after clusters, before observations — Sam)
- [x] JSON: solution_metrics section + bridge_projects list
- [x] 16 tests (metrics, bridges, health observations, reporters)

PHASE 4: SOLUTION ALIGNMENT IN DOMAIN CLUSTERS ✅ COMPLETED
----------------------------------------------------------------------
- [x] solution_alignment: float and dominant_solution: Optional[str] on Cluster
- [x] Post-hoc computation via _compute_solution_alignment() — no label propagation changes
      Team decision: dropped affinity bonus — biasing clustering toward solutions
      suppresses the divergence signal architects need (Tomás, Priya, Marcus)
- [x] Multi-solution project counts via set membership (Fatima)
- [x] Console: Align column + dominant on Members line (Sam)
- [x] JSON: solution_alignment + dominant_solution on cluster objects
- [x] 8 tests (alignment computation, integration, reporters)

PHASE 5: END-TO-END VALIDATION ✅ COMPLETED
----------------------------------------------------------------------
- [x] All wiring completed in Phases 2-4 — Phase 5 is validation only
- [x] 6 e2e tests in test_solution_e2e.py:
      --graph console (Solutions, Solution Coupling, Align), JSON (solution_metrics,
      node solutions, cluster alignment), CSV (Solutions column),
      --target-project (ConsumingSolutions populated), no-.sln backward compat, --no-graph

INITIATIVE 9 — NOT IN SCOPE (build later on this foundation):
- Solution-level blast radius ("which solutions need rebuilding?")
- Solution dependency graph (solutions as nodes)
- Solution-level CI/CD gates (depends on Initiative 7)
- .sln file modification (scatter reads, never writes)

============================================================================
INITIATIVE 10: SOW CODEBASE INDEX — ACCURATE TARGET IDENTIFICATION (v2.0)
Goal: Fix --sow mode's target identification by giving the LLM a compact
codebase index as prompt context. The LLM matches domain language to real
code artifacts instead of hallucinating project names.
Ref: docs/SOW_CODEBASE_INDEX_PLAN.md
Priority: Tier 1.9 — critical for --sow mode credibility
Estimated effort: 2-3 days across 4 phases
Depends on: Dependency graph (shipped), optionally Initiative 9 (solutions)
============================================================================

PHASE 1: BUILD THE CODEBASE INDEX (~0.5 day)
----------------------------------------------------------------------
- [x] Create scatter/ai/codebase_index.py
      CodebaseIndex dataclass (text, project_count, type_count, sproc_count,
      file_count, size_bytes)
      build_codebase_index(graph, search_scope) -> CodebaseIndex
      Compact one-line-per-project format: P:Name T:types SP:sprocs
      Namespace omitted when same as project name. File stems excluded from
      prompt (metrics-only). Shared sproc cross-reference section.
      Progressive truncation: cap types at 10/project if >100KB.
- [x] File stems NOT added to ProjectNode — collected on-demand during index
      build, keeps graph schema unchanged (Tomás's minimalist approach).
- [x] Tests: index contains all sample project names, types, sprocs.
      Index size < 100KB for 500 projects. Empty graph = empty index.
      Truncation tested with patched MAX_INDEX_SIZE.

PHASE 2: REWRITE THE SOW PROMPT (~0.5 day)
----------------------------------------------------------------------
- [x] Update parse_work_request_with_model() to accept optional CodebaseIndex
      Single template prompt with optional index section between === delimiters
      Instructs LLM: "ONLY return names that appear in the codebase index"
      Requires match_evidence per target (why this SOW language matched this artifact)
- [x] Backward compat: when index is None, index section omitted from prompt
      Log warning: "No codebase index available — target identification less accurate"
- [x] Response validation: names not in index get confidence *= 0.5 with warning
- [x] Tests: prompt includes index when provided, falls back when None,
      mock LLM returns match_evidence, names not in index get halved confidence

PHASE 3: AMBIGUITY DETECTION & EVIDENCE SURFACING (~1 day)
----------------------------------------------------------------------
- [x] Add match_evidence: Optional[str] to AnalysisTarget in scatter/core/models.py
- [x] Inline ambiguity computation (no formal dataclass — Tomás):
      _compute_ambiguity_label(targets) -> "clear"/"moderate"/"vague"
      Added ambiguity_level + avg_target_confidence fields to ImpactReport
- [x] Surface in impact report: "Target Quality: moderate (7 targets, avg confidence 0.62)"
      Show per-target match evidence in console output
      No-index warning when no targets have match_evidence
- [x] Add --sow-min-confidence flag (default: 0.3) to filter low-confidence targets
      Filtered targets logged with threshold value
      Ambiguity computed from full list before filtering
- [x] match_evidence and ambiguity flow through asdict() to JSON output
- [x] Tests: quality computed correctly for clear/moderate/vague, ambiguity in
      console, --sow-min-confidence filters targets, JSON includes evidence

PHASE 4: WIRE INTO THE PIPELINE (~0.5 day)
----------------------------------------------------------------------
- [x] In run_impact_analysis(): build index from graph before parse_work_request()
      Analyzer builds its own index (Priya) — keeps __main__.py thin
- [x] Add --dump-index flag: prints codebase index to stdout and exits
      Mode group changed to required=False; validated in __main__.py
- [x] Wire --dump-index and --sow-min-confidence into cli_parser.py and __main__.py
- [ ] End-to-end test: domain-language SOW against sample projects, verify targets
      (requires live Gemini API — tested manually with docs/sample_sow.md)
- [ ] End-to-end test: explicit-name SOW still works (regression)
      (requires live Gemini API)
- [x] --dump-index verified: all 11 sample projects appear with types/sprocs

INITIATIVE 10 — SHIPPED (2026-03-19):
Compact index format: 1,922 bytes for 11 projects (28% smaller than initial
verbose format after Sam's one-line-per-project compression).
41 dedicated tests, 724 total passing, 0 regressions.

INITIATIVE 10 — NOT IN SCOPE (build later if needed):
- RAG / vector search (index fits in single prompt at our scale)
- Code content in index (names + structure only, not method bodies)
- Multi-turn LLM refinement (start with single-shot)
- Index caching (derived from graph in milliseconds, no need to persist)
- Smarter truncation at scale (keep types matching SOW keywords — Priya)
- Hallucination rate telemetry (structured log of index validation — Marcus)

============================================================================
INITIATIVE 11: FOCUSED SOW INDEX — TWO-TIER SOLUTION-SCOPED INDEX
Target: Reduce index noise for SOW analysis by scoping to relevant solutions
Reference: docs/FOCUSED_INDEX_PLAN.md, docs/FOCUSED_INDEX_BRAINSTORM.md
Depends on: Initiative 9 Phases 1-2 (solution parsing + membership on ProjectNode)
============================================================================

PHASE 1: SOLUTION SUMMARY BUILDER (~0.5 day)
----------------------------------------------------------------------
- [ ] Create scatter/ai/solution_index.py with SolutionSummary dataclass
      (name, project_count, top_types, top_sprocs, cross_solution_refs)
- [ ] Implement build_solution_summaries(graph) — groups ProjectNodes by
      solution membership, aggregates types/sprocs, counts cross-solution edges
- [ ] Type selection heuristic: prefer types unique to one solution, skip
      short names, prefer classes/interfaces over enums
- [ ] Cross-solution edge aggregation: iterate outgoing ProjectReference edges,
      group by target solution, count — O(edges), no LLM needed
- [ ] Implement format_solution_index(summaries) -> compact text
      Format: SOL:Name P:count T:type1,type2 SP:sproc1 REF->OtherSol(N)
- [ ] Tests: correct project counts, distinctive type selection, edge counts
      match manual calculation, format under 2KB for 10 solutions

PHASE 2: FOCUSED INDEX FILTER (~0.5 day)
----------------------------------------------------------------------
- [ ] Add solution_filter: Optional[Set[str]] parameter to build_codebase_index()
      Filter graph.get_all_nodes() to nodes whose solutions intersect the filter
- [ ] Add --solutions CLI flag to cli_parser.py (comma-separated solution names)
      Compatible with --sow mode only; errors without --sow
- [ ] Wire --solutions into __main__.py: parse flag, build solution_filter set,
      pass to build_codebase_index(), skip LLM Pass 1
- [ ] Tests: filter limits index to matching projects, projects in multiple
      solutions included if any matches, None filter = full index (backward compat),
      --solutions without --sow raises error

PHASE 3: LLM PASS 1 — SOLUTION DISCOVERY (~1 day)
----------------------------------------------------------------------
- [ ] Create scatter/ai/tasks/select_solutions.py with SolutionSelection dataclass
      (name, confidence, reason, match_type: "direct"|"coupled"|"semantic")
- [ ] Implement select_solutions(sow_text, solution_index, ai_provider)
      Prompt: LLM matches SOW domain concepts to solutions using type names
      as semantic anchors + cross-solution edge signals
- [ ] Auto-include coupled solutions: after LLM selection, expand to solutions
      with cross-solution edge count >= threshold (default: 3) from any selected
      solution with confidence >= 0.5. Reduced confidence (0.5x source, min 0.3)
- [ ] Tests: mock LLM returns expected selections, auto-include expands correctly,
      no double-add, min confidence filter applied

PHASE 4: ORCHESTRATION AND UX (~0.5 day)
----------------------------------------------------------------------
- [ ] Two-pass orchestration in __main__.py:
      Pass 1: build_solution_summaries -> format -> select_solutions -> auto_include
      Pass 2: build_codebase_index(solution_filter=selected) -> existing impact flow
- [ ] Console output: show solution discovery results with reasoning
      "+ AdminPortal (high confidence) — direct semantic match"
      "! CDEX (medium confidence) — coupled: 6 refs from AdminPortal"
- [ ] JSON output: add solution_discovery section to ImpactReport
      (total_solutions, selected_solutions with confidence/reason, reduction_pct)
- [ ] Backward compat: --sow without --solutions and without .sln files uses full index
- [ ] Tests: two-pass flow produces correct focused index, --solutions skips Pass 1,
      console shows reasoning, JSON includes solution_discovery

INITIATIVE 11 — NOT IN SCOPE (build later if needed):
- Interactive solution selection (TUI pick-list)
- Solution-level risk scoring
- Multi-pass recursive expansion (re-analyze if Pass 2 discovers new solutions)
- Custom solution groupings via manifest file

============================================================================
INITIATIVE 12: XLARGE PERFORMANCE — GRAPH BUILD & CACHE OPTIMIZATION
Target: < 5 min full graph build, sub-30s cached queries at 800-project scale
Reference: docs/XLARGE_BENCHMARK_RESULTS.md
============================================================================

PHASE 1: IMMEDIATE WINS — DONE
----------------------------------------------------------------------
- [x] Drop `indent=2` from json.dump in graph_cache.py
      Shipped in 3b25a05.

- [x] Cache file identifiers in Step 4 for Step 5c reuse
      Shipped in 3b25a05. Eliminates redundant file reads + regex passes.

- [x] Scope type_usage edges to reachable project pairs
      Shipped in 3b25a05. --full-type-scan flag preserves unscoped behavior.

PHASE 2: MEASURE AND VALIDATE — DONE
----------------------------------------------------------------------
- [x] Re-run xlarge benchmark after Phase 1 fixes
      Results (--mode full, no tracemalloc, 3 runs, warmup):
        medium  (250 proj, 4.7K files):  2.60s median  (was 33.84s instrumented)
        xlarge  (800 proj, 19K files):  25.83s median
        synth   (800 proj, 30K files):  38.71s median
      All targets met: < 5 min build, sub-30s on generated xlarge.
      Gating decision: defer SQLite cache and Step 5c parallelization.

- [x] Correctness validation for scoped type_usage
      Verified: scoped and unscoped produce identical edges on sample projects.

PHASE 3: CONDITIONAL — PARTIALLY DONE, REST DEFERRED
----------------------------------------------------------------------
- [x] Threaded type extraction (ThreadPoolExecutor in Step 4)
      Shipped in 986475d. _FileExtraction NamedTuple, finditer over findall.

- [x] Optional tracemalloc in benchmark (--tracemalloc/--no-tracemalloc)
      Shipped in 986475d. Default: on for stages, off for full.

- [ ] Parallelize Step 5c (type_usage_edges) — DEFERRED
      Not needed: type_usage_edges is <0.3s at xlarge with identifier cache.

- [ ] SQLite graph cache — DEFERRED
      Not needed: JSON cache performance is adequate at current scale.

FOLLOW-UPS (from leadership review, low priority)
----------------------------------------------------------------------
- [ ] Move compute_content_hash to scatter.core (e.g. scatter.core.hashing)
      Avoids analyzers→store import coupling. Low risk, small refactor.

- [ ] Measure SHA-256 cost when capture_facts=False
      _extract_file_data hashes unconditionally. Verify <5% of stage time.

- [ ] Add --threads flag to benchmark for worker count sweeps
      Find optimal thread count for file I/O + regex workloads.

- [ ] Thread graph_patcher.py identifier cache loop (lines 368-375)
      Same sequential file-read pattern. Candidate if profiling shows need.

PHASE 4: LATER
----------------------------------------------------------------------
- [ ] Profile Louvain clustering at scale
      6.8s at xlarge (26% of total with tracemalloc off). Was hidden behind
      tracemalloc overhead (47s instrumented). Now the #2 bottleneck.

- [ ] Add medium preset benchmark to CI with regression threshold
      ~2.6s per run — cheap enough to catch perf regressions before they ship.

- [ ] Validate edge density against real WEX monolith
      Synthetic xlarge has ~14 edges/project. Real monolith may differ.
      Results may recalibrate optimization priorities.

- [ ] Fix declaration-change incremental path
      Currently 0.8x "speedup" (slower than full rebuild due to patch overhead).
      Options: fall back to full rebuild when declaration changes detected,
      or implement targeted index update (only types in changed files) to avoid
      global type index rebuild.
============================================================================
INITIATIVE: CONSOLE REPORTER REDESIGN (Target-Project Mode)
Target: Align --target-project console output with --graph mode quality
============================================================================

PHASE 1: Core Table Formatting
----------------------------------------------------------------------
- [ ] Replace --- dashes --- section headers with ==== block style matching graph_reporter
- [ ] Add summary stats block at top (target name, consumer count, scope, projects scanned)
- [ ] Replace -> arrow list with aligned columnar table for consumers
      Columns: Consumer, Solution, Coupling, Fan-In, Fan-Out, Instability, Cycle
- [ ] Remove #### separator from cli.py dispatch_legacy_output()

PHASE 2: Metrics & Detail Formatting
----------------------------------------------------------------------
- [ ] Break graph metrics out of single crammed line into table columns
- [ ] Standardize 2-space indent throughout (matching graph reporter)
- [ ] Format instability values consistently (e.g., 0.00 not 0.000)

PHASE 3: Optional Detail Sections
----------------------------------------------------------------------
- [ ] Move AI summaries (--summarize-consumers) to a separate section below table
- [ ] Format batch job status as clean tagged items [batch] like observations
- [ ] Add pipeline info as a table column when --pipeline-csv is provided

PHASE 4: Polish & Consistency
----------------------------------------------------------------------
- [ ] Add footer summary line matching graph reporter style
- [ ] Ensure filter pipeline line renders cleanly above the new format
- [ ] Update tests for new console output format
- [ ] Verify markdown reporter is not affected by console changes

PHASE 5: Follow-Up Consistency
----------------------------------------------------------------------
- [ ] Align print_impact_report() header style with ==== block convention
      Currently uses === Title === (3 equals); should match the new ==== block style.

============================================================================
INITIATIVE: HYBRID AST SPIKE — Phase 2 & Follow-Ups
Spike shipped on feature/hybrid-ast-spike (2026-03-29)
Phase 1 (spike): Config + CLI + parsers module + graph_builder wiring. DONE.
============================================================================

PHASE 2: Consumer Analyzer Wiring ✅ COMPLETED (2026-03-30)
----------------------------------------------------------------------
- [x] Wire validate_type_usage() into consumer_analyzer.py stages 4-5 via parallel.py
      AST confirmation in analyze_cs_files_batch(): regex runs first, AST only fires
      if regex found a match. Zero added cost for non-matching files.
      Class stage: validate_type_usage(content, class_name)
      Method stage: validate_type_usage(content, f".{method_name}") — dot anchors to
      member access, no paren to handle whitespace variant `.Save (`.
- [x] Thread analysis_config through find_consumers() and all call sites
      Call sites updated: cli.py (target, git, sproc), modes/impact.py,
      impact_analyzer.py (run_impact_analysis, _analyze_single_target, trace_transitive_impact).
      Resolved use_ast once at top of find_consumers(), injected into stage_config dicts.
- [ ] Extend validate_type_usage() to distinguish type references from other identifier
      positions using AST node types (object_creation, variable_declaration, base_list, etc.)
      Deferred — current byte-level substring + non-code range check is sufficient for Phase 2.
- [x] Renamed local `analysis_config` dicts to `stage_config` in consumer_analyzer.py
      Avoids name collision with the new analysis_config: AnalysisConfig parameter (Priya).

PHASE 2: Performance ✅ COMPLETED (Devon, Priya)
----------------------------------------------------------------------
- [x] Cache compiled tree_sitter.Query objects at module level in ast_validator.py
      _query_cache: Dict[str, object] = {} — keyed by query string. QueryCursor stays
      fresh per call (it's stateful). Only 2 distinct queries, so cache stays tiny.
- [ ] Profile ident_positions dict overhead vs. the old set-based path
      Hybrid path builds Dict[str, List[int]] instead of Set[str]; more allocations.
      Optimization: skip position capture for identifiers not in type_name_set (requires
      knowing type_name_set at extraction time — pipeline restructure).
- [ ] Wire --parser-mode into tools/benchmark_graph_build.py
      Run both modes, report wall-clock delta. Currently the tool doesn't accept --parser-mode.

PHASE 2: Correctness ✅ COMPLETED (Sam)
----------------------------------------------------------------------
- [x] Fix string-index vs UTF-8-byte-offset mismatch in ident_positions
      graph_builder.py: guarded with content.isascii() so 99%+ ASCII files pay zero cost.
      Only builds char→byte offset array for non-ASCII files. Per-character encode("utf-8")
      is O(n) construction, O(1) lookup per match. Tests added for non-ASCII content.

PHASE 2: Resilience ✅ COMPLETED (Fatima)
----------------------------------------------------------------------
- [x] validate_type_usage now wired and in use — barrel export is appropriate.
- [x] Add _parser = None reset in _get_parser() except block
      try/except in _get_parser() resets both _parser and _ts_language on failure
      so next call retries instead of returning stale state.

PHASE 2: Testing ✅ COMPLETED (Anya)
----------------------------------------------------------------------
- [ ] Extract _byte_positions helper to shared location (scatter/parsers/ or test utils)
      Test helper reimplements the same byte-position logic as graph_builder.
      If those diverge, unit tests pass but integration fails. Low priority.
- [x] Add concurrency test variant with different content per thread
      test_concurrent_varied_content: 4 different C# snippets across 8 threads,
      exercises query cache under contention.
- [x] Add sample projects with identifiers in comments/strings to exercise hybrid delta
      tests/fixtures/false_positive_usage.cs: PortalDataService in comment + string only.
      12 unit tests in test_consumer_ast_hybrid.py (class/method filtering, fallback,
      non-ASCII byte offsets). 2 integration tests in test_hybrid_consumer_regression.py
      (hybrid consumers ⊆ regex consumers on sample projects).
- [x] Add test for missing method_name with use_ast=True (leadership review follow-up)
      test_method_name_missing_skips_ast: verifies AST gate is skipped when method_name absent.
- [x] 3 non-ASCII tests added to test_ast_validator.py (unicode comment, unicode string, identifiers)

PHASE 2: Tooling ✅ COMPLETED (Kai)
----------------------------------------------------------------------
- [x] Guard _ts_language against uninitialized access in _run_query
      Added `if _ts_language is None: _get_parser()` at top of _run_query().

PHASE 2: Documentation (Marcus — leadership review follow-up)
----------------------------------------------------------------------
- [x] Updated validate_type_usage docstring — removed "Reserved for Phase 2",
      replaced with description of its actual role in consumer analyzer stages 4-5.


============================================================================
============================================================================
RISK ENGINE & PR RISK & SOW SCOPING ROADMAP
Source: docs/RISK_ENGINE_PLAN.md, docs/PR_RISK_SCORING_PLAN.md, docs/SOW_SCOPING_PLAN.md
Team review completed 2026-03-30. Feedback incorporated into plans.
============================================================================
============================================================================

============================================================================
INITIATIVE: UNIFIED RISK ENGINE
Goal: Single risk model (7 dimensions, weighted-max composite) consumed by
      PR risk, SOW scoping, and local dev. Three-function public API.
Ref: docs/RISK_ENGINE_PLAN.md
============================================================================

PHASE 1: Core Engine (2 weeks)
----------------------------------------------------------------------
- [ ] Add RiskProfile, RiskDimension, AggregateRisk, RiskContext, RiskLevel to models.py
- [ ] Create risk_engine.py with compute_risk_profile(), aggregate_risk(), format_risk_factors()
- [ ] Create risk_dimensions.py with 6 graph-derived dimension assessors
      (structural, instability, cycle, database, blast_radius, domain_boundary)
      NOTE: change_surface is PR-only — lives in pr_risk_analyzer, not the engine
- [ ] Implement weighted-max composite scoring with configurable dimension weights
      Document ceiling behavior: weight caps max contribution per dimension
- [ ] Add intermediate instability bracket at 0.6-0.8 (currently jumps 0.3→0.9)
- [ ] Add config validation for weights (sum, range) and thresholds (ordering)
- [ ] Add structured logging/observability from day one (dimension scores, timing, data quality)
- [ ] Handle partial/degraded data visibly (missing metrics → dimension scores 0, log warning)
- [ ] Unit tests for all 6 dimensions + aggregation + composite scoring
- [ ] Property tests: composite bounded [0,1], aggregate never lower than max target

PHASE 2: Wire into Existing Modes (1 week)
----------------------------------------------------------------------
- [ ] Replace risk_assess.py AI-only risk with graph-derived + AI-enriched
- [ ] EnrichedConsumer.risk_rating populated from RiskProfile.risk_level
- [ ] Existing --sow output gains dimension data (backward compatible)
- [ ] Add --risk-context pr|sow|local flag, --risk-details for dimension breakdown

[D] DEFERRED — after calibration data exists:
- [ ] Dimension correlation analysis (detect redundant dimensions)
- [ ] Coupling vector scoring (weight structural dimension by vector type)
- [ ] Custom risk contexts via .scatter.yaml (teams define own weight profiles)
- [ ] Consider dict-based dimension storage instead of named fields (extensibility)

============================================================================
INITIATIVE: PR RISK SCORING
Goal: GitHub Action posts risk comment on every PR. Same output locally via
      scatter --pr-risk. Zero-config for consuming teams.
Ref: docs/PR_RISK_SCORING_PLAN.md
Dependencies: Risk Engine Phase 1-2
============================================================================

PHASE 1: Core PR Risk Mode (2 weeks)
----------------------------------------------------------------------
- [ ] Add PRRiskReport, ChangedType dataclasses to models.py
- [ ] Create pr_risk_analyzer.py (~150 lines, lean — all risk logic in engine)
      Three steps: extract changed types, invoke engine, assemble report
- [ ] Implement change_surface dimension in pr_risk_analyzer (PR-only, diff-based)
- [ ] Add PRRiskConfig to config.py with threshold defaults + .scatter.yaml overrides
- [ ] Add --pr-risk mode handler to cli.py (--branch-name, --base-branch, --risk-only)
- [ ] Create pr_comment_reporter.py (markdown template with <details> collapsibles)
- [ ] Extend json_reporter.py and console_reporter.py for PRRiskReport
- [ ] Unit tests: risk level computation, threshold edges, deleted-type detection
- [ ] Golden-file snapshot tests for PR comment markdown output
- [ ] Empty result sanity check (no changed types → "no C# type changes, no risk analysis")
- [ ] Performance regression test (PR analysis completes in <10s for sample projects)

PHASE 2: GitHub Actions Integration (1-2 weeks)
----------------------------------------------------------------------
- [ ] Create .github/workflows/scatter-pr-risk.yml
- [ ] Graph cache strategy: content-keyed (csproj hash), not branch-keyed
- [ ] Failure modes: continue-on-error, fallback comment on crash, exit 0 always
- [ ] Backward compat regression test for --sow (PR mode doesn't break existing modes)

PHASE 3: Prediction Logging (1 week)
----------------------------------------------------------------------
- [ ] Add RiskPrediction dataclass
- [ ] Implement append-only JSONL prediction log
- [ ] Log every PR risk comment as a prediction record

[D] PHASE 4: Calibration (2+ weeks) — DEFERRED until prediction data exists
----------------------------------------------------------------------
- [ ] Outcome tracking: manual annotation of prediction log (quarterly review)
- [ ] Accuracy dashboard: true/false positive/negative rates
- [ ] Threshold tuning based on false positive/negative rates
- [ ] Automated webhook for build failure correlation (own scope, not bundled)

[D] DEFERRED — future enhancements:
- [ ] NuGet PackageReference support for cross-repo consumer detection
- [ ] Slack notification escalation for RED risk PRs
- [ ] Merge blocking mode (required check) — only after trust is established

============================================================================
INITIATIVE: SOW SCOPING
Goal: scatter --sow "..." --scope-estimate produces defensible effort
      estimates with risk-driven multipliers, team consultation, calibration.
Ref: docs/SOW_SCOPING_PLAN.md
Dependencies: Risk Engine Phase 1-3
============================================================================

PHASE 1: Structured Effort Estimation (2 weeks)
----------------------------------------------------------------------
- [ ] Add ScopingReport, EffortBreakdown, ConfidenceBand, DatabaseImpact to models.py
- [ ] Create effort_estimator.py: graph-derived base costs + risk-driven multipliers
      NOTE: Multipliers start at 1.0 (no effect) until calibration data exists
- [ ] Create db_impact_analyzer.py: sproc grouping, migration complexity levels
- [ ] Create effort_adjustment.py AI task (optional overlay, clearly labeled)
- [ ] Compute confidence bands from risk engine composite score
- [ ] Add --scope-estimate flag to --sow mode
- [ ] Scoping reporters: console table, JSON, markdown, CSV (for finance)
- [ ] Risk categories for planning: use reporter formatting, not a SOWRiskCategories dataclass
      Planning implications: AI-generated from dimension data, not hardcoded templates
- [ ] Unit tests: heuristic calculations, multiplier edge cases, DB impact
- [ ] SOW performance budget: measure and enforce wall-clock targets

PHASE 2: Team Mapping + Risk-Driven Multipliers (1-2 weeks)
----------------------------------------------------------------------
- [ ] Add TeamConfig to config.py (.scatter.yaml teams: section, glob patterns)
- [ ] Create team_resolver.py: consumer → team mapping, per-team risk level
- [ ] CODEOWNERS fallback parsing
- [ ] Wire teams_to_consult into ScopingReport + all reporters
      Missing config → "team ownership not configured" (visible absence)

PHASE 3: Calibration Store (2-3 weeks)
----------------------------------------------------------------------
- [ ] Create calibration_store.py: append-only JSONL, save/load predictions
- [ ] CLI: --record-actual --sow-id SOW-XXX --actual-days N
- [ ] CLI: --calibration-report (accuracy dashboard)
- [ ] Activate risk-driven effort multipliers (replace 1.0 defaults with measured values)

[D] PHASE 4: Auto-Tuning — DEFERRED until 20+ SOWs with recorded actuals
----------------------------------------------------------------------
- [ ] Multiplier tuner: detect systematic bias, suggest adjustments
- [ ] CLI: --calibration-tune (shows suggestions, human approves)
- [ ] Apply accepted adjustments to .scatter.yaml

[D] DEFERRED — future enhancements:
- [ ] Comparable SOW retrieval (topological cosine similarity on history)
- [ ] Multi-repo SOW analysis (per-repo + manual aggregation)
- [ ] Automated actuals collection from Jira/sprint data
- [ ] Per-category actuals tracking (investigation, implementation, testing)

============================================================================
CROSS-CUTTING TASKS
============================================================================

- [ ] AI prompt security review for all new risk/effort prompts
- [ ] JSON output versioning (schema version header for risk data)
- [ ] Update docs site with risk engine, PR risk, and SOW scoping pages
- [ ] Update PRODUCT_DEFINITION.md with risk roadmap summary
